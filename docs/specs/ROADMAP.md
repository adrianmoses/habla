# Roadmap

Cloud-API fork of [`hable-ya`](https://github.com/adrianmoses/hable-ya). Same
product — a voice-first Spanish acquisition agent that is simultaneously
conversational partner, pedagogical assessor, and adaptive engine — but the
three on-device models are replaced with managed APIs:

| Role | hable-ya (on-device) | habla (cloud) |
|---|---|---|
| LLM | fine-tuned Gemma 4 E4B via llama.cpp | **Claude** (Pipecat `AnthropicLLMService`) |
| STT | faster-whisper (CUDA) | **OpenAI Whisper API** |
| TTS | Piper | **Cartesia** |

Unchanged from hable-ya: Silero VAD + SmartTurn v3 (small local CPU/ONNX
models, kept in-process), the Pipecat pipeline shape, the Postgres + Apache AGE
learner model, and the learner-profile / theming / leveling logic. The
migration is scoped to the model boundary and its fallout.

| Field | Value |
|---|---|
| status | planned |
| created | 2026-07-05 |

## Features

| ID | Feature | Status | Spec |
|---|---|---|---|
| 001 | Claude LLM (`claude-sonnet-4-6`) via Pipecat `AnthropicLLMService`, replacing the llama.cpp `OpenAILLMService` (drop `base_url`/dummy `api_key`) | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 002 | Register `HABLE_YA_TOOLS` with the LLM context and emit `log_turn` via native structured tool-calling with `tool_choice: auto` (forcing suppresses the spoken reply — see spec Key Decision 3) | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 003 | Rework the tool handler to consume native function-call frames instead of buffering `LLMTextFrame`s and regex-parsing `log_turn(...)` / `[TOOL_CALL: log_turn]{...}` (reuse the existing `api_tool_calls` path in `parse_tool_calls`) | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 004 | System prompt: move the `log_turn` emission instruction into the tool definition; remove the inline plain-text-emission contract the fine-tune baked in | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 005 | Config: Anthropic model id + `ANTHROPIC_API_KEY`; remove `llama_cpp_url`, `llm_model_name`, and the Gemma `chat_template_kwargs.enable_thinking=false` hack (disable thinking / low effort for voice latency) | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 006 | Replace the llama.cpp warmup ping-loop with a lightweight managed-API health check (or drop it) | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 007 | STT → OpenAI transcription API (`gpt-4o-transcribe`, replacing faster-whisper CUDA `medium`); Spanish language config | implemented | [007-stt-tts-cloud](007-stt-tts-cloud/spec.md) ([decision](007-stt-tts-cloud/decision.md)) |
| 008 | TTS → Cartesia (`sonic-3`, replacing Piper `es_ES-davefx-medium`); owner-supplied Spanish voice | implemented | [007-stt-tts-cloud](007-stt-tts-cloud/spec.md) ([decision](007-stt-tts-cloud/decision.md)) |
| 009 | Deployment: delete the llama.cpp GPU compose service and drop all `nvidia` GPU reservations — app container becomes CPU-only (Postgres + AGE `db` service unchanged); also removes `cuda_bootstrap.py` and loads `.env` via python-dotenv | implemented | [009-gpu-free-deploy](009-gpu-free-deploy/spec.md) ([decision](009-gpu-free-deploy/decision.md)) |
| 010 | Dependency cleanup: drop `faster-whisper`, `piper-tts`, the `finetune` extra (`torch`, `unsloth`, `transformers`), and `jupyterlab`; promote `anthropic` to core (`openai`/`cartesia` already core) | implemented | [010-ondevice-cleanup](010-ondevice-cleanup/spec.md) ([decision](010-ondevice-cleanup/decision.md)) |
| 011 | Remove on-device-only tooling: `finetune/` package, `download_model.py`, `benchmark_*` scripts, `replay_placement.py`, the fine-tune notebook (`cuda_bootstrap.py` removed in #009) | implemented | [010-ondevice-cleanup](010-ondevice-cleanup/spec.md) ([decision](010-ondevice-cleanup/decision.md)) |
| 012 | Re-baseline the `eval/` harness: validate that Claude + prompt reproduces recast + `log_turn` fidelity, replacing the fine-tuned-vs-untuned-Gemma comparator (keep the Opus recast/session judges) | implemented | [012-eval-rebaseline](012-eval-rebaseline/spec.md) ([decision](012-eval-rebaseline/decision.md)) |
| 013 | Cloud round-trip latency re-benchmark: measure network TTFT vs local, re-tune `smart_turn_stop_secs` / VAD `stop_secs` for the added hop | implemented | [013-cloud-latency](013-cloud-latency/spec.md) ([decision](013-cloud-latency/decision.md)) |
| 014 | API resilience & cost: rate-limit handling, retry/backoff (tenacity already present), and per-turn token-cost observability | planned | — |
| 015 | Product/docs update: on-device → cloud posture — learner utterances now leave the device (privacy), README, OVERVIEW non-goals | implemented | [015-cloud-docs](015-cloud-docs/spec.md) ([decision](015-cloud-docs/decision.md)) |
| 016 | Session auth & single-session enforcement: gate the public `/ws/session` WS endpoint (token/API-key), enforce the single-tenant one-active-session assumption the shared-LLM `register_function` handler already depends on (concurrency cap / reject second connection), complete startup fail-fast for OpenAI/Cartesia keys + `CARTESIA_VOICE_ID` (today only Anthropic is verified; the others crash on first turn), and make `/health` reflect live cloud-API reachability (today it re-checks only the DB and returns `200 ok` while sessions are broken). Removes the open cost-DoS on the metered paid APIs — the one true public-exposure blocker. | implemented | [016-session-auth](016-session-auth/spec.md) ([decision](016-session-auth/decision.md)) |
| 018 | Web frontend auth + deploy: the `web/` Vite SPA is a dev-only artifact predating the cloud deploy — nothing builds/serves it in prod (Caddy only reverse-proxies the API), and its WS client opens `/ws/session` with no token so #016's fail-closed auth rejects it (`1008`). Wire the session token into the client (subprotocol/`?token=`, no server change), serve the built SPA behind the existing prod Caddy, and refresh the stale (llama.cpp/Gemma) `web/README.md`. No new UI surface. | implemented | [018-web-frontend-deploy](018-web-frontend-deploy/spec.md) ([decision](018-web-frontend-deploy/decision.md)) |
| 017 | Deployment hardening: non-root multi-stage Dockerfile (`uv sync --no-dev`, install `es_core_news_sm` so vocab tracking doesn't silently degrade, add a `HEALTHCHECK` wired to `/health`); a prod compose overlay (restart policy, resource limits, app healthcheck, db not host-exposed, reverse-proxy + TLS for `wss://`); replace the hardcoded `hable_ya:hable_ya` DB creds / empty-string secret defaults with injected secrets + config-layer validation; a GHCR image build-push CD job (CI is test-only today); a `pgdata` backup/restore path (none exists) and a redeploy/migration runbook (migrations currently run coupled to app boot; `scripts/init_db.py` exists as a standalone step but is unwired). | implemented | [017-deploy-hardening](017-deploy-hardening/spec.md) ([decision](017-deploy-hardening/decision.md)) |
| 019 | Learner-progress read API: the learner model is fully *written* (per-turn `log_turn` → Postgres relational tables + AGE graph; placement/leveling band changes at session end) but nothing outside the dev-gated `/dev/learner` + `/dev/observations` endpoints can *read* it. Add authed production HTTP endpoints exposing the profile snapshot (band, sessions completed, error/vocab aggregates), session history, band history, and recent themes — the data bridge every stubbed frontend surface in #020 needs. | implemented | [019-learner-progress-api](019-learner-progress-api/spec.md) ([decision](019-learner-progress-api/decision.md)) |
| 020 | Web frontend: wire the dead surfaces to live data. Home's nav links `Progreso`/`Historial`/`Ajustes`, the profile avatar, six recent-topic cards, and "ver todo →" are inert (`Home.tsx:112-134,511,527-560` — no `href`/`onClick`, or a "próximamente" toast, while styled `cursor: pointer`); the stats tiles (racha / nivel / última sesión) and Session's `NIVEL A2` badge are static placeholders. Build the progress/history/settings screens on #019 (these are the #046-deferred surfaces #018 explicitly kept out of scope); interim quick fix: stop dead links from looking clickable. | implemented | [020-web-frontend-surfaces](020-web-frontend-surfaces/spec.md) ([decision](020-web-frontend-surfaces/decision.md)) |
| 021 | Learner identity: the UI greets a hardcoded "Ana" (`Home.tsx:141`, placeholder pending the learner model) and the backend has no user concept — a schema-enforced singleton learner (`learner_profile CHECK (id = 1)`), ephemeral `uuid4` session ids, and a single shared session token. Minimum: owner-configurable learner name/profile surfaced in the UI (stays single-tenant, consistent with OVERVIEW's "not multi-tenant" non-goal). Explicitly decide whether multi-user accounts remain a non-goal — supporting them needs a schema migration (drop the `id=1` CHECK), per-user auth, and per-learner session routing. | implemented | [021-learner-identity](021-learner-identity/spec.md) ([decision](021-learner-identity/decision.md)) |
| 023 | Targeted conversations: let the learner/operator steer a session toward a **debate**, **role-play**, or **interview** (default **open**) with an optional freeform topic/scenario, instead of today's random cooldown-filtered theme pick (`get_session_theme`). A mode is a `Theme` factory filling only the `## Topic:` block (`render.py`) — the recast / `log_turn` / register machinery is untouched, so the learner-model loop stays safe by construction. **No level-gating of the kind** (any mode at any band; only the tutor's vocab/grammar scale, via the existing band blocks); config is **per-session at start** (query params on `/ws/session`, mirroring `_extract_token`; no mid-session switch) and **ignored until the learner is calibrated** (cold-start diagnostic preserved). Persists a nullable `sessions.mode` so #020 can surface it. Backend is independently shippable / `curl`-testable ahead of #020's picker UI. | implemented | [023-targeted-conversations](023-targeted-conversations/spec.md) ([decision](023-targeted-conversations/decision.md)) |
| 024 | Response latency ("time to first word"): measure how long the learner takes to start speaking after the tutor finishes a question/statement — the fluency signal neither #013 (bot-side latency) nor `analiza` (no interlocutor anchor) captures. A per-session `ResponseLatencyObserver` anchors on `BotStoppedSpeakingFrame` (observation-time stamp; armed only when the learner isn't already speaking, so barge-ins don't measure) and measures to the VAD-corrected `VADUserStartedSpeakingFrame` onset; the `log_turn` handler attaches the consume-once value as `extra["response_latency_ms"]`, persisted to the JSONL sink and the (previously never-written) `turns.raw_extra` JSONB. Always-on, no migration, no pedagogical use; read-API/UI surfacing deferred to #019/#020. | implemented | [024-response-latency](024-response-latency/spec.md) ([decision](024-response-latency/decision.md)) |
| 022 | Knowledge-graph read path (or honest downgrade): the AGE graph is upserted on every turn but write-only — no runtime code ever queries it; all live adaptivity (prompt profile, theming, leveling) is driven by the relational aggregates (`learner_profile`, `turns`, `error_counts`, `vocabulary_items`). Either wire graph reads into adaptation (e.g. error-pattern neighborhoods informing theme/prompt selection) or re-document the graph as an inspection-only artifact; also fix README's broken `turns` query (`created_at` → `timestamp`; no `l1_reliance_score` column exists). | implemented | [022-knowledge-graph-read](022-knowledge-graph-read/spec.md) ([decision](022-knowledge-graph-read/decision.md)) |
| 025 | `vocab_strengths` is recency, not strength — and the tutor is told otherwise. Surfaced by #020, but it runs deeper than the client-side workaround shipped there: `aggregations.py:81-84` selects the top-N lemmas by `vocab_last_seen` alone (the docstring at line 60 says so plainly — "selection is last_seen DESC only"), `read.py:58-66` does the same in SQL (`ORDER BY last_seen_at DESC`), and `render.py:292-293` then renders that list into the **tutor's system prompt** as `- Vocabulary strengths: …`. So a word produced twice yesterday is presented to Claude as a strength over one produced nine times last week. The asymmetry looks unintentional: `error_patterns` immediately above (`aggregations.py:73-79`) *is* frequency-ranked (count DESC, last_seen as tiebreaker). Decide what "strength" means — production frequency (`production_count` is already tallied and already in the payload), retention/recency, or a composite — then make the name, the SQL, the snapshot and the prompt agree. Note `/dev/learner` shares the query, and #020 sorts client-side by `production_count` (`vocabByProduction` in `web/src/lib/format.ts`) as a UI-only patch to be removed once the source is fixed. | planned | — |
| 026 | `l1_reliance` / `speech_fluency` carry no "no data" state. Both fall back to a neutral `0.5` when the rolling window is empty (`aggregations.py:23-24,62-71`) and that value is then emitted unconditionally in two places: the tutor's system prompt (`render.py:285-288`, `- L1 reliance: 0.50` / `- Speech fluency: 0.50`) and `/api/learner` (`read.py:88-89`). A learner who has never spoken is therefore described to Claude as mid-scale on both signals, and every consumer must infer emptiness from elsewhere — #020's Progreso gates its meters on `sessions_completed > 0`, a proxy a zero-turn session slips past. Minimum: make absence representable (nullable signals, or a `turns_observed` count carried on the snapshot and the payload) and suppress or caveat the prompt lines when there is no window. Touches the rendered prompt, so the cold-start byte-identity tests need re-baselining. | planned | — |
| 027 | Constrain the error-category vocabulary. `errors[].type` in the `log_turn` tool schema (`hable_ya/tools/schema.py:65`) is a bare `{"type": "string"}` with no enum, and the prompt only offers an example (`render.py:234`) — so `error_counts.category` holds whatever Claude writes that turn. English snake_case, Spanish prose and novel phrasings were all observed in a single seeded list during #020. Everything downstream inherits the noise: aggregation splits synonyms into separate rows (`aggregations.py:73-79`), so a learner making one error consistently can have it ranked below a rarer one; the tutor prompt's "Known error patterns" line lists the raw strings back (`render.py:290-291`); and #020's `errorLabel()` maps the 12 curated slugs in `eval/agent/personas/schema.py:31-46` and prettifies the rest. Constraining the schema sharpens the learner model's own aggregates, not just UI labels. It is a tool-contract + prompt change with existing rows written under the old freedom, so it needs a mapping/backfill decision and an escape hatch for genuinely novel categories. | planned | — |
| 028 | Read path for #024's `response_latency_ms`. Measured per turn and persisted to `turns.raw_extra` (`ingest.py:158`) but read by nothing — no endpoint, no aggregate, no UI; deliberately deferred by both #019 and #020. Surfacing it needs a **null-aware** aggregate API-side first: the key is absent by design for first turns and barge-ins, so "not measured" must not collapse into zero and drag a mean down. Then a #019-style field on the profile payload and a #020-style Progreso readout — subject to #026's "no data" decision, which this shares. | planned | — |
| 029 | The orientation docs describe a system that no longer exists. `OVERVIEW.md`'s Audit Notes are frozen at the pre-implementation state: line 78 calls Postgres/AGE "planned" and says "No DB code is implemented yet" while `aiosqlite` is "currently vendored" (it was dropped); line 110 says the learner model is "schema-only" with "no knowledge graph, no profile persistence, no error-pattern aggregation"; line 111 says `db/connection.py` and `db/hable_ya_db.py` are empty with "no schema, no migrations". All four migrations and the whole learner package have shipped since. `ARCHITECTURE.md` carries `status: inferred` and a component map still listing stubs and the deleted `finetune/` package. Both docs are what a new contributor or agent reads first, and both are now actively misleading — #020, #021 and #022 each corrected the one or two lines they touched and left the rest, so the rot is being routed around rather than fixed. Needs a decision on what these documents are *for* (living reference vs. dated audit snapshot) before an edit pass, since "keep them current" is what has already failed three times. | planned | — |
| 030 | `scripts/seed_dev_learner.py` has no test, and silently diverged because of it. #022 found that `--reset` truncated the relational tables but not the AGE graph — `TRUNCATE` does not touch AGE — so every reseed left stale graph nodes contradicting the relational data beside them. `tests/conftest.py`'s `clean_learner_state` had always cleared both; the seed script's copy of the same statement did not, and nothing compared them. Fixed in #022, but the class of defect stands: the seeder is the only dev-facing data path with no coverage, it duplicates reset SQL that lives in two other places, and it is what every frontend and inspection surface is eyeballed against — so when it drifts, it drifts under exactly the work that would otherwise catch it. Minimum: a test that the seeded end state satisfies the properties its docstring promises (streak shape, null `ended_at`, off-enum error categories, graph mirroring), and a single shared definition of "reset the learner state" instead of three copies. | planned | — |
| 031 | Four of five migrations have a `downgrade()` that has never been executed. #021 added the suite's first upgrade→downgrade round-trip (`tests/test_learner_display_name_migration.py`), and it covers `-1` only — so `f1e6a742b90c` is the sole revision whose reverse path has ever run. The other four are untested code on the deploy path, and two are not merely unexercised but suspect: **`bd55d203ae25`** drops the `learner_knowledge` graph *and* all six learner tables (`vocabulary_items`, `error_counts`, `error_observations`, `turns`, `sessions`, `learner_profile`) behind a dollar-quoted `DO $$ … $$` block, and **`20c019e280a9`** runs `DROP EXTENSION IF EXISTS age;` with no `CASCADE` — which succeeds only if the graph was already dropped by the revision above it, i.e. correct in a full `downgrade base` chain and a failure in any partial one. Nobody has ever found out. Two honest resolutions, and the spec should pick one rather than split the difference: extend #021's round-trip pattern down the whole chain (a fresh database, `upgrade head` → `downgrade base` → `upgrade head`, asserting schema and graph presence at each end), or **delete the `downgrade()` bodies and declare the migrations forward-only**, which is a defensible posture for a single-tenant app that has never rolled one back. What should not persist is the current state: reverse paths that read as supported, are documented by their own presence, and would be discovered to be broken during an incident. | planned | — |

## Status Values

- `planned` — not yet started
- `in-progress` — spec written, implementation underway
- `implemented` — decision record complete
- `deprecated` — removed from product

## Revision History

| Date | Change |
|---|---|
| 2026-08-04 | Added **#029–#030** from #022's "Spec Gaps Exposed", following the precedent #020 set with #025–#028. **#029 — the orientation docs describe a system that no longer exists.** `OVERVIEW.md`'s Audit Notes still call Postgres/AGE "planned" with "No DB code is implemented yet" and `aiosqlite` "currently vendored" (line 78), the learner model "schema-only" with no knowledge graph or profile persistence (line 110), and `db/connection.py` / `db/hable_ya_db.py` empty with no schema or migrations (line 111) — all false since the learner package and four migrations shipped. `ARCHITECTURE.md` is still `status: inferred` with a component map listing stubs and the deleted `finetune/` package. The reason this is a roadmap item rather than an edit: **#020, #021 and #022 each corrected the one or two lines they touched and left the rest**, so three specs in a row routed around it. The item therefore starts with a decision about what these documents are *for* — living reference or dated audit snapshot — because "keep them current" is precisely what has already failed. **#030 — `seed_dev_learner.py` has no test and silently diverged because of it.** #022 found `--reset` truncating the relational tables but not the graph (`TRUNCATE` does not touch AGE), leaving stale nodes contradicting the relational data on every reseed; `conftest.py`'s `clean_learner_state` had always cleared both, and nothing compared the two copies. Fixed in #022, but the class stands: the seeder is the only dev-facing data path with no coverage, it duplicates reset SQL living in three places, and it is what every frontend and inspection surface is eyeballed against — so it drifts underneath the very work that would otherwise catch it. Both `planned`, neither blocking. Not added: #022's third gap (a spec deferring a decision to a measurement should say which comparison would settle it) — that is a lesson for the spec workflow, recorded in the decision record, not product work. |
| 2026-08-04 | Added **#031** — the orphaned follow-up from #021's decision record, which flagged it as a roadmap candidate and then never got an entry. Verifying it before writing it down made it worse than "untested": #021's round-trip test covers `-1` only, so `f1e6a742b90c` is the **only** revision whose `downgrade()` has ever executed, and two of the remaining four are actively suspect. `bd55d203ae25` drops the graph *and* all six learner tables behind a dollar-quoted `DO $$ … $$` block; `20c019e280a9` runs `DROP EXTENSION IF EXISTS age;` **without `CASCADE`**, which only succeeds if the graph was already dropped by the revision above it — correct in a full `downgrade base` chain, a failure in any partial one, and never once exercised either way. The item deliberately offers two resolutions and asks the spec to pick one: extend #021's pattern down the whole chain, or delete the `downgrade()` bodies and declare the migrations forward-only (defensible for a single-tenant app that has never rolled one back). The state to avoid is the current one — reverse paths that read as supported, are documented by their own existence, and would be found broken during an incident. `planned`, not blocking. |
| 2026-08-04 | Spec 022-knowledge-graph-read **implemented**; #022 → implemented. **The AGE graph is settled as an inspection artifact, not an input to adaptation** — and the answer is evidenced rather than argued. **Measured** (`graph-cost.json`, 50 turns, two runs agreeing to 0.4%): the graph writes are 3.1ms p50 and **58.5% of the ingest transaction**, but 0.15% of a turn against #013's ~2.05s floor; 13.4 cypher round trips per turn. The first run was discarded and a warm-up added after the relational mean (18ms vs p50 2ms) turned out to be the one-off spaCy model load — reporting it would have flattered the graph. **Candidate queries run against the real graph**, not reasoned about: error-pattern co-occurrence — *the roadmap's own suggested adaptation* — returns `[]` because no `ErrorPattern`↔`ErrorPattern` edge exists, while SQL answers it with one join on `error_observations.turn_id`; `ENGAGED_WITH` keeps **no history** (`MERGE … SET r.last_at` overwrites, so 3 engagements leave 1 edge with the latest timestamp — strictly lossier than `sessions`); zero `VocabItem`↔`Scenario` edges; and the one query the graph *can* serve returns output identical to `error_counts` from an indexed table. Only two edge types exist in practice. **Shipped:** `graph._fetch_cypher` + `graph_summary` (the graph's first reader, with a `json.loads` agtype decoder — agtype arrives as `str` with JSON quoting, so the existing `int(str(...))` helper works for counts and mangles labels); a `graph` block on the dev-gated `/dev/learner` reporting known labels at `0` rather than omitting them, so a writer that stops is visible; **OQ2 resolved by moving the graph upserts after the relational commit**, best-effort with a `graph_failed` sink counter — justified on robustness not speed, since decorative work should not roll back a turn's real learner state; `OVERVIEW.md`'s headline claim reworded from "knowledge-graph–based learner model" to relational-with-an-inspection-graph; `ARCHITECTURE.md`'s `[INFERRED: uncertain] AGE schema not yet designed` replaced with the **shipped** schema plus the three properties that make the proposed adaptation impossible (star not network, counters duplicated on node *and* edge, no engagement history); `README.md:154`'s unrunnable `turns` query fixed (`created_at` → `timestamp`, `l1_reliance_score` → `fluency_signal`/`l1_used` — neither ever existed); and `tests/test_readme_snippets.py`, which executes every README SQL block and was verified by reintroducing the original defect (2 tests fail naming the statement). **The new reader immediately found a bug**: `seed_dev_learner.py --reset` truncates the learner tables but `TRUNCATE` does not touch AGE, so every reseed left stale graph nodes contradicting the relational data beside them — now cleared, and the seeded aggregates mirrored into the graph through the real writers so the inspection block is not permanently empty in dev. 538 pytest (from 524), 6 e2e, ruff + mypy clean; 37 prompt tests unmodified and `git diff --stat -- hable_ya/pipeline/` empty. **Two spec divergences**: OQ4's opt-out marker was **not built** — all nine README statements execute (each block runs in order on one connection, so its own `SET search_path` covers the cypher after it), and unused machinery is the exact thing this spec exists to remove; and OQ2's stated default ("a small fraction of the ingest transaction") proved unanswerable as phrased, so the failure mode decided it instead. **Unblocks #021 Key Decision 4's multi-user accounting** — the graph re-model is not on anyone's critical path, because the graph is not load-bearing. Follow-up candidates: nothing tests `seed_dev_learner.py` (which is how it and the graph drifted apart); `OVERVIEW.md`'s Audit Notes (lines 78/110/111) still describe the pre-implementation state — "No DB code is implemented yet", `aiosqlite` — deliberately left as a spec Non-Goal; and `ARCHITECTURE.md` remains `status: inferred` with a component map listing stubs and the deleted `finetune/` package. See [decision](022-knowledge-graph-read/decision.md). |
| 2026-08-04 | Spec 022-knowledge-graph-read **approved**; ready to implement. All four Open Questions resolved at their recommended default. **OQ1: honest downgrade** — document what the graph is, ship an inspection read so "write-only" stops being true, correct the two docs that oversell it, and keep the writes so a future spec inherits history rather than an empty graph. The two validation steps still run *first* (measure the per-turn write cost; write the candidate-query table checking each plausible adaptation query against what SQL already answers), because if that table turns up a query the current graph can answer and SQL cannot, it is a finding worth surfacing before the doc edits land — and it belongs in the decision record either way. **OQ2: decide from the measurement**, a deliberate deferral with an owner, an input and a default (leave the writes in the ingest transaction if the graph portion is a small fraction of it; move them out if not — decorative writes should not be able to fail a turn's real persistence). **OQ3: a graph block on the dev-gated `/dev/learner`**, which already shares `learner/read.py` with production so the two cannot drift. **OQ4: the README-snippet test covers cypher as well as SQL**, with an explicit opt-out marker that must state *why* — an unexplained skip is how the broken `turns` query survived. Next: implement against the spec, starting with the measurement and the query table, before any documentation edit. |
| 2026-08-04 | Spec 022-knowledge-graph-read drafted on branch `spec-knowledge-graph-read-022`; #022 → in-progress. Exploration sharpened the roadmap line in three ways. (1) **There is no read path to switch on.** `graph.py`'s only primitive, `_run_cypher`, calls `conn.execute` and returns `None` — it structurally cannot return rows, so a read path is net-new code, not a wiring change. All five functions in the module are writers. (2) **The write cost is concrete**: the ingest transaction issues `2 × distinct_categories + 2 × lemmas` cypher round trips per turn (`ingest.py:63-78`) — ~20 statements for a typical turn — inside the same transaction as the relational writes, so a graph failure can roll back real learner state. (3) **The roadmap's own adaptation suggestion is not currently available**: "error-pattern neighborhoods" require edges that do not exist. The shipped graph is a *star* around `(:Learner {id: 1})` — no `VocabItem`↔`VocabItem`, no `ErrorPattern`↔`ErrorPattern`, no `ErrorPattern`↔`Scenario` edge. Every traversal available today is one hop from a node whose counters duplicate an indexed relational table. So adaptation is not "write a query" but "design a graph worth querying, backfill it, beat the relational policy, re-baseline the prompt tests" — at least #023-sized, on a subsystem with no demonstrated value. Spec therefore frames **the decision as the deliverable**, with the recommended branch being the honest downgrade: document what the graph is, ship an inspection read so "write-only" stops being true, correct the two docs that oversell it, and keep the writes so a future spec inherits history rather than an empty graph. Also in scope and independently worth doing: `README.md:154` documents a `turns` query that **cannot run** (`created_at` does not exist — the column is `timestamp` — and there is no `l1_reliance_score` column at all; that signal is computed in `aggregations.py`, never stored per-turn), and `ARCHITECTURE.md:234` still calls the AGE schema `[INFERRED: uncertain] … not yet designed` though `bd55d203ae25` shipped it. Confidence **Medium** — mechanics are High (166-line module, existing `clean_graph` fixture, surgical doc edits, no new dependency, no provider), but OQ1 is a judgment that should be evidenced before the doc edits land, so the spec requires two validation steps first: measure the per-turn graph write cost, and write out a candidate-query table checking each plausible adaptation query against what SQL already answers. Four OQs: the branch itself (recommend downgrade); whether the per-turn writes stay in the ingest transaction (recommend deciding after measuring, biased to moving them out — decorative writes should not fail a turn's real persistence); where the inspection read lives (recommend extending the dev-gated `/dev/learner`); and whether the README-snippet test covers cypher as well as SQL (recommend both, with an explicit opt-out marker). Unblocks #021 Key Decision 4's multi-user accounting, which is explicitly blocked on this decision. Independent of #025–#028. |
| 2026-08-04 | **Correction to the #021 implemented entry below, plus one defect it hid.** That entry claimed "Playwright is not available in this environment" and deferred the three browser checks. **This was false** — Playwright 1.56.0 is installed via pipx at `~/.local/bin/playwright` with browsers in `~/.cache/ms-playwright`; the detection only looked in `node_modules/.bin`, `web/node_modules/.bin`, the default `python3` and `npx --no-install`, none of which see a pipx user install. #020's "Playwright is available locally" was right all along. Running the checks then **falsified spec #021's own OQ3 rationale**: the 40-character bound was justified as fitting "the 96px serif greeting without wrapping past two lines at the SPA's `maxWidth: 520`", but `maxWidth: 520` is on the `<p>` beneath the hero, not the `<h1>`, which spans the full `1.2fr` grid column — a 40-character name rendered **five** lines and pushed the "Empezar a hablar" CTA below the fold. Fixed by having `greetingLine` greet by **first name only** (`'Maximiliana Guadalupe Fernández Ochoa Ru'` → hero `buenas noches, Maximiliana.`); the 40-character bound, Ajustes' field and the avatar initial all still use the full value. Re-verified in chromium at 1440×900: 2 lines, CTA at 693px, and the other two checks pass — empty state renders `Buenas noches.` with a blank-but-navigable avatar circle, and Ajustes → Home propagates the name with no document reload (`performance.now()` confirms). Zero console errors throughout. 51 vitest (from 47), pytest/ruff/mypy unchanged and green. **Two lessons recorded in the decision record:** a numeric bound justified by a layout claim belongs in the acceptance criteria, not the rationale — the criteria as written ("sees it in Home's greeting") were satisfied at five lines with the CTA off-screen; and a negative tooling-availability result deserves the same scepticism as a negative test result. |
| 2026-08-04 | Spec 021-learner-identity **implemented**; #021 → implemented. Shipped all six changes on branch `spec-learner-identity-021`: migration `f1e6a742b90c` (nullable `learner_profile.display_name`, revising head `c7f3a9b21d84`, `CHECK (id = 1)` untouched); a new `hable_ya/learner/identity.py` holding the pure `normalize_display_name` plus the single writer; `display_name` on `GET /api/learner`, read from the raw profile row; **`PATCH /api/learner` — the first write endpoint on the production HTTP surface** — behind the same `require_api_token` Bearer gate; `apiPatch` + `greetingLine`/`avatarInitial` in the SPA; and a `Tu nombre` panel in Ajustes. Both `PLACEHOLDER … spec #021` comments deleted — `Home.tsx`'s `<em>Ana</em>` and `AppShell.tsx`'s `A` are gone, the last invented claims #020 left on the surface. All 13 acceptance criteria addressed, all three OQs at their approved resolutions. **The UI-only guarantee held structurally**: `git diff --stat -- hable_ya/pipeline/` is empty and the 37 prompt tests pass unmodified, because the name is read outside `LearnerProfileSnapshot` and so has no path to `render.py`. 524 pytest (from 469) + 47 vitest (from 37) passing, ruff + mypy clean, `tsc --noEmit && vite build` green. E2E verified over real HTTP against real Postgres: padding trimmed, `Ángela` round-trips, 401 on missing/wrong token for the write, 422 on 41 chars / control char / RTL override / empty body with the stored value unchanged, whitespace clears to SQL `NULL`. **Three spec divergences, all recorded**: the spec's `Field(max_length=40)` sketch contradicted its own OQ3 (which bounds the *trimmed* value), so the rule lives in the pure helper with a loose model guard; `PATCH {}` is a 422 rather than a silent clear (unspecified, and the destructive reading is the wrong default for a one-field resource); and `greetingLine` returns `{lead, name}` rather than a flat string, so Home keeps its clay-deep `<em>` and line break. **Two verification gaps worth carrying forward**: (1) **Playwright is not available in this environment**, contrary to the spec's assumption — the three browser checks (empty-state hero, Ajustes→Home propagation with no reload, 40-char wrap at `maxWidth: 520`) were **not run**, and #020's record credits browser verification with three defects no unit test found, so this should be re-checked before the next deploy; (2) the round-trip test written here is the suite's **first executed `downgrade()`** — the spec cited "the existing migration tests' pattern" and no such pattern exists, meaning the four prior migrations all carry `downgrade()` bodies that have never run. Multi-user recorded as a **decided non-goal with the costed reversal** in both `OVERVIEW.md` and `ARCHITECTURE.md`, replacing the bare assertion. Follow-up candidates: executed downgrades for the whole migration chain (or declare them forward-only); a shared profile read for the SPA if the shell ever needs more than an initial (AppShell's avatar currently costs a duplicate `/api/learner` GET on three screens). See [decision](021-learner-identity/decision.md). |
| 2026-08-04 | Spec 021-learner-identity **approved**; ready to implement. All three residual Open Questions resolved at their recommended default: `PATCH /api/learner` (one resource, one path — a `/profile` child under a router already prefixed `/api/learner` reads redundantly); with no name set, the greeting renders alone with a blank avatar circle (the only option that claims nothing — a neutral placeholder word would reintroduce exactly the invented-identity defect this spec removes); and validation is trim / ≤40 characters / no Unicode `Cc`/`Cf`, with an empty trimmed value meaning "clear it" (no character-class allowlist — the value is parameterized into SQL, never reaches cypher, and React escapes on render, so the bound is a layout constraint rather than a security one). The three questions are kept in the spec with their alternatives intact: they are what the acceptance criteria defend against. Next: implement against the spec, starting with the migration. |
| 2026-08-04 | Spec 021-learner-identity drafted on branch `spec-learner-identity-021`; #021 → in-progress. Closes the last invented claim #020 left on the surface — the hardcoded `<em>Ana</em>` (`Home.tsx:141`) and avatar `A` (`AppShell.tsx:115`), both carrying explicit "spec #021" placeholder comments. Three decisions taken before drafting: (1) storage is a nullable `learner_profile.display_name` column (migration revising head `c7f3a9b21d84`) plus a `PATCH /api/learner`, **not** a config setting — an env var is only configurable by redeploying, which would leave #020's Ajustes screen able to display a name but never edit it; (2) **UI-only** — the name never enters the tutor prompt, so `render.py` stays byte-identical and the cold-start prompt-identity tests need no re-baselining; (3) **multi-user is a standing non-goal**, with the reversal cost written into the spec rather than built. Verifying that cost against the code widened it past the roadmap line's "drop the CHECK + per-user auth + session routing": `error_counts` is `PRIMARY KEY (category)` and `vocabulary_items` is `PRIMARY KEY (lemma)` (`bd55d203ae25:80-98`) — today one learner's vocabulary table *is* the vocabulary table, so both need composite PKs; and the AGE graph is modelled wrong for it in a way that is **blocked on #022** — `VocabItem`/`ErrorPattern` nodes are global and carry their counters on the *node* (`graph.py:85,118`) while the per-learner edge carries its own, so two learners producing *viajar* would inflate a shared node, and re-modeling it means redesigning a graph nothing reads yet. Plus ~41 SQL sites, a real auth system replacing `auth.py`'s 29-line boolean, and #016's global one-session cost bound reopening per-learner. Key design decision: `display_name` is read from the raw profile row, **not** carried on `LearnerProfileSnapshot` — anything on the snapshot is one edit from `render.py`'s `## Learner` block, so keeping it off makes "never reaches the prompt" structural rather than a comment (same argument #023 used for confining a mode to `## Topic:`). Confidence **High** — every piece has a shipped precedent (4th migration of the same shape, #019's auth dependency reused verbatim, #020's pure-helper-plus-Vitest pattern), no new dependency, and **no deferred live spike**: neither the read nor the write path touches a provider. Residual OQs are naming/empty-state only: PATCH path (recommend `/api/learner`), what renders with no name (recommend greeting-only + blank avatar circle — the one option that claims nothing), and validation bounds (trim, ≤40 chars, no control characters). Housekeeping the change forces: `clean_learner_state` must reset `display_name` or a PATCH test leaks a name across the suite. |
| 2026-07-26 | Added #025–#028 from #020's "Spec Gaps Exposed" — the four follow-up candidates its decision record recorded but left unowned. Verifying each against the code before writing it down **widened two of them**: (1) `vocab_strengths` was logged by #020 as an API-ordering misnomer worked around client-side, but `aggregations.py:81-84` selects on recency alone and `render.py:292-293` feeds that list to the **tutor's system prompt** as "Vocabulary strengths" — so the wrong claim is made to Claude on every turn, not just to the UI, and `error_patterns` right beside it *is* frequency-ranked, making the asymmetry look accidental (→ **#025**); (2) the neutral `0.5` for `l1_reliance`/`speech_fluency` likewise reaches the prompt unconditionally (`render.py:285-288`), not only `/api/learner` — a learner who has never spoken is described to Claude as mid-scale on both (→ **#026**). #027 (constrain `errors[].type`; the open vocabulary splits synonyms across aggregate rows, so a consistent error can rank below a rarer one) and #028 (null-aware read path for #024's `response_latency_ms`) carry over as scoped. All four are `planned`, none blocks #021. |
| 2026-07-25 | Spec 020-web-frontend-surfaces implemented; #020 → implemented. Shipped all 7 workstreams on branch `spec-web-frontend-surfaces-020`, closing the loop #019 and #023 left open (both had shipped a contract with no consumer): in-repo ~40-line history router + typed API client with per-surface `{data,error,loading}` hooks mirroring `read.py`; Home's tiles / topic cards / nav / avatar / "ver todo" reading live data with the `próximamente` toast deleted; three new screens (Progreso / Historial / Ajustes) in María's register; Session's live band badge + a mode/topic picker on #023's contract; and 37 Vitest tests over the pure logic that moved client-side. **No backend change, no migration, no new runtime dependency** (`web/` stays `react` + `react-dom`; vitest is dev-only). All 26 acceptance criteria built as written, all five OQs at their approved resolutions. **Step 0's validation gate earned its place**: seeding a realistic DB (`scripts/seed_dev_learner.py`, committed) and curling all three endpoints confirmed they support every specced screen (no #019 amendment) and caught that `top_vocab`/`vocab_strengths` are ordered by `last_seen_at`, not `production_count` — `vocab_strengths` is a misnomer, worked around client-side. **Browser verification (Playwright, available locally) then found three defects no unit test would have** (commit `01ea6aa`): (1) `sessionStorage` fires no same-tab event, so hooks fetched once on mount — pasting a token populated nothing until reload, and a tokenless load's guaranteed 401 raced in and cleared the token just typed (fixed with a token-change signal + `useSyncExternalStore`, and hooks now skip fetching with no token); (2) per-screen "did this 401?" recovery **cannot work** — clearing the token re-runs every hook and wipes the error before it is observed, stranding the user on a blank screen (replaced with `useAuthGuard`, one rule stated about the token); (3) Progreso drew half-full meters for a learner with zero turns, because the API returns a neutral `0.5` for `l1_reliance`/`speech_fluency` with no turns — inventing exactly the kind of claim this spec exists to remove. 37 vitest + 469 pytest passing, ruff + mypy clean; all four screens rendered against seeded *and* empty DBs with zero console errors; default WS URL confirmed byte-identical to pre-#020. **First spec in the series whose verification did not defer to a keyed host** — `require_cloud_secrets` only checks truthiness and the read path touches no provider. Deferred: a live per-mode turn and the deployed Caddy hop over TLS. Spec gaps → follow-up candidates: `vocab_strengths` misnamed; the two 0–1 signals carry no "no data" state; `errors[].type` has no enum so `error_counts.category` is an open vocabulary (constraining it would sharpen the learner model, not just labels); #024's `response_latency_ms` still has no read path. Learner name + avatar initial deliberately untouched — #021 owns them. See [decision](020-web-frontend-surfaces/decision.md). |
| 2026-07-25 | Spec 020-web-frontend-surfaces **approved**; ready to implement. All five Open Questions resolved with the recommended default: a **minimal in-repo router** (`useRoute()` over `history.pushState`/`popstate` + `navigate()`, ~40 lines — `react-router-dom` rejected as disproportionate for five flat routes; runtime deps stay `react` + `react-dom`); the **live session is not URL-addressable** (stays in-app state on `/` — a reload on `/sesion` would auto-grab the mic and open a paid socket; one pushed history entry makes Back exit cleanly); the **mode/topic picker is in scope** as the severable last workstream (WS6), with the rule that if ever cut the topic cards must become non-interactive rather than decorative-but-live; **Vitest as a dev dep** for the pure helpers only (no jsdom/RTL — the streak calc is the silently-wrong-for-months case); and topic cards deduped **most-recent-per-`theme_domain`, capped at six** from a `limit=20` pull. Confidence stays **Medium**: OQ resolution closed one of the three drivers, but the design-led nature (three net-new screens, no ported #046 reference) and the untested payload sufficiency against a populated DB remain. Next: validation step 1 (seed a realistic DB, `curl` all three endpoints, confirm the payloads support the screens *before* building them), then WS1. |
| 2026-07-25 | Spec 020-web-frontend-surfaces drafted on branch `spec-web-frontend-surfaces-020`; #020 → in-progress. Closes the loop opened by the 2026-07-12 audit: #019's read API shipped with **no consumer**, so the SPA still renders a hardcoded `A2 · inicial`, "aún no hay" sessions, and six invented topic cards to a learner who may have been promoted to B1. Exploration confirmed the transport path is already fully open — `Caddyfile` `handle /api/*` + `vite.config.ts` `/api` proxy (both landed by #018/#019), `lib/token.ts` already holds the Bearer secret, and #023's `?mode=&topic=` parser is fail-safe server-side (`_extract_conversation_config` → `parse_conversation_config`, unknown mode → `open`, never raises) — so the picker is a **client-only** change. Scope: 7 workstreams — router shell + typed API client/hooks mirroring `hable_ya/learner/read.py`, Home's tiles/cards/nav wired live, three new screens (Progreso / Historial / Ajustes), Session's live band badge + mode/topic picker, and (pending OQ4) Vitest over the pure helpers. Non-goals: learner name + avatar initial (**#021** owns the schema question), AGE reads (#022), cost metrics (#014), any new endpoint or migration, responsive redesign. **Key finding — the deferred-live-spike problem mostly does not apply here**: `require_cloud_secrets` (`api/main.py:53`) only checks truthiness and the read path touches no provider, so all of #020 runs locally on placeholder OpenAI/Cartesia values; only `warmup_llm`'s 1-token Anthropic ping (gating `app.state.ready`, hence the `/health`-driven CTA) needs a real key. Also surfaced: Home gates its CTA on `hasToken` regardless of server config, so a dev with `session_auth_disabled=true` must still paste a dummy token — Ajustes' token controls make that workable without touching the auth gate. Confidence **Medium** — mechanics are High (shipped API, open transport, no persistence), but four of five Open Questions change the shape of the work (router dep vs ~40 in-repo lines; picker in-or-out; Vitest vs build-gate-only; topic-card dedup), this is the first substantially *design*-led spec in the series (three net-new screens with no ported #046 reference), and no screen has rendered against a populated DB. Validation before building: resolve the OQs, seed a realistic DB and `curl` all three endpoints, confirm the local dev loop. Follow-up recorded: #024's `response_latency_ms` has no read path (`raw_extra` needs a null-aware aggregate on the API side first), so it stays out of #020. |
| 2026-07-24 | Spec 024-response-latency implemented; #024 → implemented. Shipped on branch `spec-response-latency-024`, matching the spec with **zero divergences**: new `hable_ya/pipeline/processors/response_latency.py` (`ResponseLatencyObserver` — frame-id-dedup ring, one-shot anchor armed on clean bot stop iff learner silent, disarmed on bot restart, VAD back-corrected onset, negatives discarded, consume-once `pending_ms` + `records` deque) plus narrow wiring: `build_observers`/`build_pipeline_task` gain `extra_observers` (always attached; `latency_debug` still gates only the #013 diagnostics), `make_log_turn_handler(latency=)` pops the pending value into `extra["response_latency_ms"]`, the session route builds the shared per-session instance, and `TurnIngestService._insert_turn` now writes `raw_extra` (`json.dumps(obs.extra)`) — closing the confirmed latent gap that `extra` was silently dropped on the DB path since `bd55d203ae25` (all historical rows hold the default `'{}'`, no backfill needed). 469 pytest passing with DB up (zero skips; 15 new: 10 observer state-machine, 3 handler, 2 raw_extra round-trip, +2 runner wiring), ruff + mypy clean. Deferred (same class as #016–#023): a live keyed session confirming plausible `response_latency_ms` values in `runtime_turns.jsonl`/`turns.raw_extra` — full boot fail-fasts without the three cloud-API keys. Follow-up hooks for #019/#020: aggregate over `raw_extra` in SQL or promote a column; absent key = "not measured" (first turn, barge-ins), so aggregation must be null-aware. See [decision](024-response-latency/decision.md). |
| 2026-07-24 | Spec 024-response-latency drafted + approved on branch `spec-response-latency-024`; #024 → in-progress. New feature from a user ask ("measure the time to first word — when I first respond to a question"): learner response latency, the inverse of #013's bot-side metric. Exploration confirmed the live pipeline already carries both anchors — `BotStoppedSpeakingFrame` (timestamp-less → stamped at observation time, same posture as Pipecat's own `UserBotLatencyObserver`) and `VADUserStartedSpeakingFrame` (`timestamp` − `start_secs` = true onset, mirroring Pipecat's stop-side back-correction) — while `analiza` structurally cannot (solo recording, no interlocutor anchor; its leading-silence gap is file-relative). Design: per-session `ResponseLatencyObserver` (one-shot anchor armed on clean bot stop iff learner not already speaking — covers barge-in + mid-turn pause/resume; disarmed on bot restart so multi-part replies measure from the final stop) + a consume-once `pending_ms` slot the `log_turn` handler pops into `TurnObservation.extra["response_latency_ms"]` (safe by pipeline ordering: onset strictly precedes that turn's STT→LLM→`log_turn`); persistence closes the latent gap that `turns.raw_extra` (JSONB, migration `bd55d203ae25`) was never written. Open questions resolved at spec time (owner pre-approved implementation in-session): always-on (learner metric, not `latency_debug`-gated); `raw_extra` over a new column (no migration); server-side stop stamp accepted (client-buffer skew is consistent turn-over-turn, fine for trends). Non-goals: first-*word*-vs-first-sound STT cross-reference, analiza integration, read-API/UI surfacing (#019/#020), pedagogical use. Confidence High. |
| 2026-07-15 | Spec 023-targeted-conversations implemented; #023 → implemented. Shipped on branch `spec-targeted-conversations-023`: two new modules — `hable_ya/pipeline/conversation.py` (`ConversationConfig` + fail-safe `parse_conversation_config`) and `hable_ya/learner/modes.py` (`build_mode_theme`, the debate/role-play/interview/open→topic `Theme` factories with per-band `target_structures` hints for `debate`/`interview` only) — plus narrow wiring: a `conversation_config` param on `build_session_prompt` routing the theme via the factory **in the calibrated branch only** (`SessionPrompt.mode` added); `_extract_conversation_config` on `/ws/session` mirroring `_extract_token`; `start_session(mode=)` → a new nullable `sessions.mode` column (migration `c7f3a9b21d84`, revising head `99507a1b3027`); and `mode` surfaced through #019's `session_history` (auto-propagates to `/api/learner/sessions`). **Safe by construction confirmed**: a mode fills only the `## Topic:` block, so register/rubric/recast/`log_turn` are untouched and the cold-start byte-identity tests pass unchanged. One documented divergence: the `ConversationMode` literal lives in `eval/fixtures/schema.py` beside `CEFRBand` (not `pipeline/conversation.py`) to avoid a learner→pipeline import — `_Strict` (`extra="forbid"`) `Theme`/`SystemParams` also forced the mode value to flow separately (`ConversationConfig`→`SessionPrompt.mode`→`sessions.mode`) rather than on the `Theme`. 410 pytest passing (DB up, +37), ruff + mypy clean. Deferred (as #016/#018/#019): the live keyed `wss://` per-mode turn confirming Claude adopts each posture — full boot fail-fasts without the three cloud-API keys. Unblocks #020's picker UI. See [decision](023-targeted-conversations/decision.md). |
| 2026-07-15 | Spec 023-targeted-conversations **approved**; ready to implement. All five Open Questions resolved with the recommended default: flat `?mode=&topic=` query params (mirroring `?token=`); one freeform `topic` string per mode (structured sub-params deferred); minimal per-band `target_structures` (authored for `debate`/`interview` only, `role_play`/`open` empty); `theme_domain` = a `"<mode>: <topic>"` slug (bare mode name when no topic); and `open` + an explicit topic overrides the random pick (byte-identical to today when no topic). Next: implement against the spec. |
| 2026-07-15 | Spec 023-targeted-conversations drafted on branch `spec-targeted-conversations-023`; #023 → in-progress. New feature from a pre-#020 design discussion: steer a session into debate / role-play / interview (default open) + an optional freeform topic. Key realization from exploration — the per-band pools in `hable_ya/learner/themes.py` are *already* role-plays/debates (`"Tú eres el camarero…"`, B2 `"Debate con el estudiante…"`) picked at random by `get_session_theme`; a "mode" is just an alternative `Theme` factory filling the `## Topic:` block (`render.py:295-303`), so the recast / `log_turn` / register contract is structurally untouched and the learner loop stays safe. Design locked in brainstorm: three orthogonal axes (mode / topic / band), **no kind-gating** (only the tutor's vocab/grammar scale, via the existing band-driven register/rubric/recast blocks), **per-session at start** via `/ws/session` query params (mirrors `_extract_token` at `session.py:58`; no mid-session switch), **ignored until calibrated** (`is_calibrated_async` gate → cold-start diagnostic preserved), four modes (open default, debate, role-play, interview). Injection is narrow: a `conversation_config` param on `build_session_prompt` (`builder.py:64`, calibrated theme branch at `builder.py:91-99`) selecting a mode `Theme` factory vs today's random pick; a nullable `sessions.mode` column (new alembic migration revising head `99507a1b3027`) persisted via `start_session` (`ingest.py:79`); surfaced through #019's read module for #020. Confidence High on mechanics (one new pure module + one param + a query-param read + one nullable column; no new deps), with mode-prompt Spanish copy + a live 3-mode smoke deferred to a keyed spike as in prior specs. Blocks/feeds #020; depends on the calibration gate; relates to #019 (session payload gains `mode`); orthogonal to #021/#022. |
| 2026-07-12 | Spec 019-learner-progress-api implemented; #019 → implemented. Shipped three authed read endpoints under `/api/learner` (profile snapshot, paginated session history with per-session `turn_count`, band history), gated by the #016 shared secret via an `Authorization: Bearer` dependency (`require_api_token`), fail-closed. Two extractions keep it DRY: `hable_ya/auth.py::authorize_token` (WS `_authorized` now delegates; a regression test pins they agree) and `hable_ya/learner/read.py` (`profile_payload`/`session_history`/`band_history`), which the re-pointed `/dev/learner` also consumes — deleting ~120 lines of duplicated SQL and removing dev/prod drift. Same-origin serving: a `/api/*` reverse-proxy handle in the prod `Caddyfile` (ordered before the catch-all so the SPA file_server can't shadow it; `caddy validate` green) + a `/api` Vite dev proxy, no CORS. **Matched the spec with no design divergences** (all five Open Questions built at their approved defaults); the only shape subtlety was re-nesting the shared payload under `/dev/learner`'s existing `profile` key to keep `test_dev_endpoints.py` green. New `tests/test_learner_api.py` (26 tests: auth DB-free + payload/pagination/band-history against real Postgres via `clean_learner_state` + the extraction regression); full suite 373 passing (DB up), ruff + mypy clean. Deferred (same class as #016/#017/#018): the live `curl` through a fully-booted app + Caddy over TLS — a full boot fail-fasts without the three cloud-API keys, so the request path was verified via `ASGITransport` against real Postgres instead; the Caddy hop + lifespan `app.state` stay for a consolidated deployed-host smoke pass. Unblocks #020. See [decision](019-learner-progress-api/decision.md). |
| 2026-07-12 | Spec 019-learner-progress-api **approved**; ready to implement. All five Open Questions resolved with the recommended defaults: `Authorization: Bearer` token transport; `/api/learner*` prefix; no CORS middleware (same-origin via Caddy in prod, Vite proxy in dev); server-side streak deferred (client computes it from the sessions list); `/dev/learner` kept but re-pointed at the shared read module. Next: implement against the spec. |
| 2026-07-12 | Spec 019-learner-progress-api drafted on branch `spec-learner-progress-api-019`; #019 → in-progress. Exploration confirmed the read model already almost entirely exists: the dev-gated `GET /dev/learner` (`api/routes/dev.py:44-160`) assembles the full profile snapshot (via `LearnerProfileRepo` + `is_calibrated_async`) plus top errors/vocab, recent theme domains, band history, and recent turn bands — it's just mounted only when `dev_endpoints_enabled`, unauthenticated, and "do not use in production" (`api/main.py:141-144`). Spec promotes this to three authed production endpoints under `/api/learner*` (profile, paginated session history, band history), reusing #016's shared-secret token via an `Authorization: Bearer` dependency (extract `_authorized` from `session.py:75` into a shared `authorize_token` helper) and factoring the dev endpoint's inline SQL into a shared read module so dev/prod never drift. Only genuinely new code: a `sessions ⋈ COUNT(turns)` history query and the Bearer dependency — no schema change, no new dep, no writes. Confidence **High** (data layer + auth pattern + a near-complete reference endpoint all already run in prod paths); residual uncertainty is confined to Open Questions (Bearer vs `?token=` → Bearer; `/api/*` prefix; same-origin-via-Caddy + Vite-proxy so no CORS; defer server-side streak; keep `/dev/learner` re-pointed at the shared module). Blocks #020; depends on #016 + the ported learner model; orthogonal to #021/#022. |
| 2026-07-12 | Post-deploy product audit added #019–#022 (all planned). Trigger: three user-visible gaps on the deployed app — dead frontend links, a default learner name, and no visible session logging / skill adaptation. Findings: the backend learner loop is **already wired and load-bearing** (every native `log_turn` tool call runs a per-turn Postgres transaction — `turns` row, `error_counts`, spaCy-lemmatized `vocabulary_items`, AGE graph upserts — with `runtime_turns.jsonl` as a redundant dev sink; the system prompt is DB-adaptive once a placement `band_history` row exists, and `end_session` runs real placement/leveling band writes), but it is **entirely invisible**: no production read API exists (only dev-gated `/dev/learner` + `/dev/observations`), so the SPA ships only the two #046 screens (Home, Session) with every other surface a static stub — `Progreso`/`Historial`/`Ajustes`/avatar/topic-cards/"ver todo" dead (`Home.tsx`), stats tiles and the `NIVEL A2` badge hardcoded, learner name hardcoded "Ana". Identity is a schema-enforced singleton (`learner_profile CHECK (id = 1)`, no user concept — consistent with the single-tenant non-goal but unconfigurable). One claim/reality gap: the AGE knowledge graph is written every turn but never read — adaptivity is relational-only (plus a broken README `turns` SQL example). Split into #019 (read API), #020 (frontend surfaces), #021 (learner identity), #022 (graph read path or downgrade). Suggested order: #019 → #020; #021 and #022 independent; the perceived "no adaptation" should mostly dissolve once #019/#020 surface the existing loop. |
| 2026-07-10 | Spec 018-web-frontend-deploy implemented; #018 → implemented. Shipped on branch `spec-web-frontend-deploy-018` (recommended option for every Open Question, no server-side change): token pasted once → `sessionStorage` and carried on the `Sec-WebSocket-Protocol` subprotocol (`client.ts`/`token.ts`; Home paste field gates the CTA; `Session.tsx` clears a `1008`-rejected token so Home re-prompts); `handle`-block `Caddyfile` (WS/health reverse-proxied, static SPA with `try_files` fallback — mutually exclusive so the file_server can't shadow the upgrade); `web/Dockerfile` (Node → `caddy:2`, `dist/` at `/srv`) published by `release.yml` as `<repo>-web` + a `ci.yml` web build gate; `web/README.md` rewritten for the cloud stack. **Two implementation discoveries:** (1) the SPA build was already broken on a fresh checkout — `Home.tsx` imported `web/src/lib/health.ts` but the repo `.gitignore`'s bare `lib/` rule (Python template) silently ignored `web/src/lib/`, so the file was never committed; fixed by anchoring the rule to `/lib/` and restoring the `useHealth` hook (CI had no web job to catch it — now it does); (2) a rejected token needed a client recovery path, not specified by #016/#018. Verified locally: `npm run build` green, prod `compose config` + `caddy validate` pass, web image builds with the SPA at `/srv`, and an HTTP routing run confirmed `/` + deep-links serve the SPA while `/health` + `/ws/session` route to the proxy (502 against a dead upstream = not static-shadowed); `git grep` clean of token literals under `web/`. Deferred to a human on a deployed host (Medium-confidence live spikes needing a public domain + real keys): the served-page authenticated `wss://` turn through Caddy and the same over real TLS. See [decision](018-web-frontend-deploy/decision.md). |
| 2026-07-10 | Spec 018-web-frontend-deploy implementation underway on branch `spec-web-frontend-deploy-018`; #018 → in-progress. Open Questions resolved with the operator's recommended options: **OQ1** = Option B (paste-once token → `sessionStorage`, never bundled) transported via the **`Sec-WebSocket-Protocol` subprotocol** (kept out of URLs/access-logs; `token_urlsafe` chars are valid subprotocol tokens, so no `?token=` fallback needed); **OQ2** = Option A (Caddy `file_server` serves the built `dist/`, `/ws/session*` + `/health` still `reverse_proxy`'d — via mutually-exclusive `handle` blocks so the static handler can't shadow the WS upgrade); **OQ3** = a custom Caddy+SPA image (`web/Dockerfile`, Node build → `caddy:2` with `dist/` at `/srv`) built + pushed by `release.yml` as `<repo>-web`, so the operator pulls a prebuilt image rather than hand-building on the host. Landed: restored the missing `web/src/lib/health.ts` `useHealth` hook (`Home.tsx` imported it but it was never committed here, breaking the build — a build-gate prerequisite); `web/src/lib/token.ts` + a paste-once token field on `Home.tsx` (CTA gated on token presence); `client.ts` attaches the token as the sole WS subprotocol; `Session.tsx` passes it and clears the stale token on a `1008` reject; `handle`-block `Caddyfile`; `web/Dockerfile` + `.dockerignore`; `docker-compose.prod.yml` `caddy` service → `ghcr.io/adrianmoses/habla-web:latest`; `release.yml` `web-image` job (scoped gha cache) + `ci.yml` `web` typecheck/build gate; `web/README.md` rewritten for the cloud stack. **No** server-side change (`_extract_token` already accepts the subprotocol). Deferred live spikes (need a public domain + real keys): served-page authenticated `wss://` turn through Caddy, and `GET /` SPA + `/ws/session` upgrade coexistence (no handler shadowing). Decision record to follow via `ss-decision`. |
| 2026-07-10 | Spec 018-web-frontend-deploy drafted on branch `spec-web-frontend-deploy-018`; #018 → planned. Surfaced during the first Hetzner deploy: the repo's `web/` Vite+React SPA (from the ported spec #046) is unusable on a deployed host — (1) nothing builds or serves it (Dockerfile/compose/Caddy/CI never reference `web/`; prod Caddy only reverse-proxies the API's `/ws/session`+`/health`, no `StaticFiles` mount), (2) its WS client opens a bare `new WebSocket('/ws/session')` with no token, so #016's fail-closed auth rejects every connection with `1008`, and (3) `web/README.md` still documents the deleted on-device stack (llama.cpp/Gemma/spec-021). #017 had explicitly deferred frontend deploy as a Non-Goal. Spec scopes the two hard blockers + doc refresh only (no new UI surface — the #046-deferred screens stay deferred): attach the session token to the handshake (recommend `Sec-WebSocket-Protocol` subprotocol over `?token=` to avoid logging it; no server change — `_extract_token` already accepts both), and serve the built `dist/` behind the existing prod Caddy (`file_server` + SPA `try_files`, `reverse_proxy` retained for the WS+health paths). Confidence Medium: mechanics standard but two open questions gate implementation — the token-exposure model (bundle-baked `VITE_SESSION_TOKEN` vs. a paste-once `sessionStorage` field; recommend the latter for a public URL) and serve-location (Caddy `file_server` vs. FastAPI `StaticFiles`; recommend Caddy to keep the API image lean). Depends on #016 (token contract) + #017 (Caddy edge); orthogonal to #014. |
| 2026-07-08 | Spec 017-deploy-hardening implemented; #017 → implemented. Shipped all six workstreams on `spec-deploy-hardening-017` (7 commits: spec + WS1–WS6). WS1 multi-stage non-root Dockerfile (`uv sync --no-dev --no-install-project`, `es_core_news_sm` baked in, uid 10001, `libstdc++6`, curl-free urllib `HEALTHCHECK`); WS2 `docker-compose.prod.yml` overlay (Compose `!reset`/`!override` to strip host ports, GHCR image, limits, Caddy auto-ACME `wss://`) + base `./data`→named `appdata` volume, base stays TLS-free; WS3 `require_secure_db_credentials` lifespan fail-fast on empty/placeholder DB password (dev opt-out `allow_default_db_credentials`, base true/prod false) + 6 tests; WS4 `release.yml` GHCR CD separate from test-only `ci.yml`; WS5 AGE-safe `pg_dump`/`pg_restore` scripts; WS6 `docs/RUNBOOK.md`. Building surfaced two design misses the plan didn't catch: `eval/` is a RUNTIME dep of `hable_ya` (must ship in the image) and pipecat downloads NLTK `punkt_tab` at first import (pre-baked at build). The spec's central Medium-confidence risk resolved decisively — a plain `pg_dump -Fc` round-trips the Apache AGE graph cleanly (extension + `ag_graph`/`ag_label` via `pg_extension_config_dump` + label tables + vertex rows all survived dump→fresh-DB→restore), so no volume-snapshot fallback needed. 295 pytest passing (52 db tests skip without db), +6 new, ruff+mypy clean; Docker build + non-root import + both compose configs + backup/restore all verified locally. Deferred live spikes (need real keys / a public domain): healthy-boot end-to-end, `wss://`-through-Caddy turn, GHCR push. Spec gap → follow-up candidate: move the shared `eval.fixtures.schema`/`eval.scoring.recast` symbols into `hable_ya` so the runtime image needn't ship `eval/`. |
| 2026-07-07 | Spec 017-deploy-hardening drafted on branch `spec-deploy-hardening-017`; #017 → in-progress. Closes the deployment-readiness pair (#016 was the security blocker; #017 is the operational hardening that assumes it). Six independent workstreams: (1) multi-stage non-root Dockerfile (`uv sync --no-dev`, bake `es_core_news_sm` so vocab tracking stops silently degrading to `[]` at `vocabulary.py:44`, `HEALTHCHECK` reusing #016's `/health` semantics unchanged); (2) `docker-compose.prod.yml` overlay layered over the dev-friendly base — restart policy, resource limits, app healthcheck, `db` host-port publish removed (internal-only), reverse-proxy + TLS terminating `wss://`; (3) inject the hardcoded `hable_ya:hable_ya` DB creds + extend #016's lifespan fail-fast to reject empty/placeholder creds outside dev (kept out of the `Settings` validator so keyless CI `Settings()` still builds); (4) a GHCR build-push CD job separate from test-only `ci.yml`; (5) `pg_dump`/`pg_restore` backup/restore scripts that round-trip the AGE graph; (6) a deploy/migration runbook (boot-coupled `upgrade_to_head()` today vs standalone `scripts/init_db.py`). Confidence Medium — mechanics are standard but each is a cross-cutting infra integration point needing live validation (non-root writability of `runtime_turns.jsonl`/spaCy cache, AGE-graph backup round-trip, real `wss://` handshake, placeholder-rejection without breaking CI). Resolved by owner: **Caddy + automatic ACME TLS**, terminated in the **prod overlay only** (local `docker compose up` on the base file stays plain `ws://localhost:8000` — no proxy/cert/domain, dev loop untouched); **boot-coupled migrations kept** (`upgrade_to_head()` at boot; `init_db.py` documented as the manual alt, not wired). Remaining questions also resolved: secret transport = **env injection** baseline (compose `secrets:` optional overlay, documented not default); image tagging = `sha-<short>` + `latest` on `main`, `v<version>` on git tags. Spec **approved** — ready to implement (workstream 1: the Dockerfile). Explicitly NOT #014 (no retry/backoff/cost-metering; `/health` stays reactive — the pipeline-internal `ErrorFrame` blind spot remains #014's). Scope: single-host compose only (no k8s/GitOps/managed-secrets); CD publishes the image, operator deploys per runbook. |
| 2026-07-07 | Spec 016-session-auth implemented; #016 → implemented. Shipped the four-layer design: per-session services (`build_session_services`) so the shared-LLM `register_function` clobber can't corrupt across connections (concurrency now safe → cap is policy, not a correctness lock); preemptive newest-wins cap with the lock held only for the pointer swap + identity-guarded clear (a stale session can never block a new client); `idle_timeout_secs` reaping (was `None`); and a fail-closed shared-secret auth gate (`secrets.compare_digest`, subprotocol/query token, never logged) checked before `accept()`. Plus lifespan fail-fast on empty OpenAI/Cartesia/`CARTESIA_VOICE_ID` (in the lifespan, NOT a `Settings` validator — CI builds `Settings()` keyless) and a reactive `degraded` `/health` signal. 341 pytest passing (52 previously-skipped DB tests ran with the db up), +13 new auth/enforcement unit tests, ruff+mypy clean. Live spike passed: token-gated session drove a full STT→Claude→Cartesia turn returning spoken Spanish (`out.wav` ~197 KB). Additive divergences (helpers extracted for testability; `make_test_wav.py` + a trailing-silence endpointing fix to `voice_client.py`/`make_test_wav.py` needed to run the spike; token docs; incidental `.env.example` 4.0→3.0 fix). Not live-exercised: two-client preemption + killed-client reap (covered by offline helper tests). Gap → #014: reactive `/health` can't see pipeline-internal `ErrorFrame` provider failures. |
| 2026-07-06 | Spec 016-session-auth drafted on branch `spec-session-auth-016`; #016 → in-progress. Design centers on removing the *need* for a session lock rather than building one: Layer 1 gives each session its own `log_turn` handler (per-session LLM service — cheap, no model load) so the shared-`register_function` clobber (`session.py:80`) can't corrupt across connections, demoting the single-session cap from a correctness lock to a cost policy; Layer 2 enforces the cap by **preemption** (newest connection evicts the incumbent) under a lock held only for the pointer-swap, never the session lifetime — so a stale/half-open session can never block a new client and there's no death-timeout to tune; Layer 3 sets `runner.py`'s `idle_timeout_secs` (currently `None`) to reap abandoned sessions proactively; Layer 4 adds a shared-secret auth gate checked pre-`accept()`. Plus startup fail-fast on empty OpenAI/Cartesia keys + `CARTESIA_VOICE_ID` (today only Anthropic is warmed) and a reactive `degraded` signal in `/health`. Confidence Medium — auth/fail-fast/idle-timeout are low-risk; the eviction + per-session-teardown path needs a live spike. Open questions: fail-closed-vs-open on unset token (proposed: fail-closed + dev opt-out), reactive-vs-active health probing (proposed: reactive), per-session isolation scope for STT/TTS (proposed: all three), token transport (subprotocol or query param). Overlaps #014 on cost visibility — #016 does the spend-bounding cap; #014 owns real token metering. |
| 2026-07-06 | Deployment-readiness assessment (post-#013, now CPU-only) added #016 + #017. Findings: DB bootstrap is genuinely production-grade — a fresh `docker compose up` auto-runs Alembic migrations, creates the AGE extension + graph, seeds nodes, warms the LLM, and serves with no manual steps; `.env` secret handling, DB/Anthropic startup fail-fast, and dev-endpoint gating are all solid. The blocker: `/ws/session` is public and unauthenticated on `0.0.0.0:8000` with no rate-limit / concurrency cap / cost metering → open cost-DoS on the paid APIs, compounded by the shared-LLM `register_function` clobber corrupting turn logging under concurrent connections (the single-active-session assumption is load-bearing but unenforced). Plus partial fail-fast (only Anthropic checked at boot), a `/health` that re-checks only the DB, and no container/compose/CD/backup hardening (root user, dev deps in image, no restart/limits/TLS, no image publish, no pgdata backup). Split into #016 (auth + single-session enforcement + fail-fast + honest health — the blocker) and #017 (image/compose/CD/backup hardening). #014's cost/retry scope partly overlaps #016's cost-metering; to be reconciled when #014/#016 are specced. Recommend #016 first. |
| 2026-07-06 | Spec 013-cloud-latency implemented; #013 → implemented. Built `scripts/benchmark_latency.py` (per-stage cloud TTFB harness), `PerStageLatencyObserver` (live per-stage `latency_debug` split from Pipecat `MetricsFrame`s, wired via a new `build_observers()` helper), `hable_ya/runtime/latency.py` (pure p50/p95 stats), + offline tests (274 passing). Live run (20 iters, `013-cloud-latency/latency.json`): STT 711/1229, LLM TTFT 1179/1581, TTS 161/204 ms (p50/p95); summed network floor ≈2.05s p50 / 3.0s p95. Key finding: that floor (LLM TTFT + STT dominated) already exceeds the p50≤1.5s / p95≤2.5s target on the network legs alone, so endpointing tuning cannot hit it — re-tuned only to not add to the floor: `smart_turn_stop_secs` 4.0→3.0 (trim on-device carry-over ceiling; only bites on uncertain/trailing learner turns), `vad_stop_secs` kept 0.5 (evidence-backed no-op). Divergences: observer wired via extracted `build_observers()` (testable); TTS TTFB measured via a capture pipeline, not direct `run_tts()` (streaming Cartesia pushes audio downstream, doesn't return it). Spec gap → follow-up candidate: sub-target latency needs faster STT/LLM or streaming-partial STT (a #013 Non-Goal). Outstanding human check: live `voice_client.py`+`latency_debug` session to confirm 3.0s doesn't cut off learner pauses. |
| 2026-07-06 | Spec 013-cloud-latency drafted on branch `spec-cloud-latency-013`; #013 → in-progress. Exploration confirmed the turn-taking defaults (`smart_turn_stop_secs=4.0`, `vad_stop_secs=0.5` in `config.py`) are on-device carry-overs never re-tuned for the cloud hop, and that instrumentation is partial: `latency_debug` → `UserBotLatencyObserver` logs only end-to-end ms, while `PipelineParams(enable_metrics=True)` already emits per-service `MetricsFrame`/`TTFBMetricsData` that goes uncaptured. Spec adds three deliverables — a `scripts/benchmark_latency.py` harness (direct-`Services`, per-stage STT/LLM-TTFT/TTS-TTFB p50/p95, replacing the deleted on-device `benchmark_latency.py`), a per-stage `latency_debug` breakdown in `runner.py` sourced from `MetricsFrame`, and an evidence-gated re-tune of the two `stop_secs` defaults — plus fixing a stale `voice_client.py` llama.cpp docstring. Confidence Medium: mechanics are High but the tuning decision depends on live-measured p50/p95 and an owner-confirmed latency budget (Q1), and the "vs local" comparison is a documented historical reference, not a reproducible A/B (Q3). |
| 2026-07-06 | Spec 015-cloud-docs implemented; #015 → implemented. Docs + `.env.example` only (zero code — `git status` shows only those files + the spec dir). `.env.example` rewritten to mirror `config.py` (dropped `LLAMA_CPP_URL`/`WHISPER_*`/`PIPER_*`/gemma/`HF_TOKEN`; added the three provider keys + `CARTESIA_VOICE_ID`); README retitled `habla`/cloud with a working `docker compose up` run path (Download/Serve-model sections removed, History note added); OVERVIEW/ARCHITECTURE posture, dependency, data-flow, and constraint sections corrected, the "Not a cloud-hosted service" non-goal inverted, and a **privacy** statement added (audio→OpenAI, transcript→Anthropic, reply→Cartesia leave the device). Two extra migration-caused dangling refs fixed in passing (OVERVIEW's `finetune/format.py` DPO citation; ARCHITECTURE's eval-run llama.cpp leg → Anthropic SDK). Scoped to cloud posture only; a full re-baseline of the `inferred` design docs (stubbed-runtime language predating specs 029/049) is flagged as a follow-up. Verified: grep gate clean, `.env`↔`config.py` diff exact, `docker compose config` parses; CI-scoped ruff + mypy clean, 258 pytest passing. Closes the #001–#015 migration series. |
| 2026-07-06 | Spec 015-cloud-docs drafted on branch `spec-cloud-docs-015`; #015 → in-progress. Docs-only slice closing the migration series: all consumer-facing docs still describe the deleted on-device system (README's `download_model.py` + `docker compose up llama` + GPU/`HF_TOKEN` setup; `.env.example`'s `LLAMA_CPP_URL`/`WHISPER_*`/`PIPER_*`/gemma vars that `config.py` no longer reads; OVERVIEW's "Not a cloud-hosted service" non-goal; ARCHITECTURE's llama.cpp CUDA deps). Spec rewrites README + `.env.example` + the posture/deps sections of OVERVIEW/ARCHITECTURE to the cloud stack, and records the new **privacy** posture (learner audio→OpenAI, transcript→Anthropic, reply→Cartesia now leave the device). Scoped to the cloud posture only — a full re-baseline of the `status: inferred` design docs (stubbed-runtime language predating specs 029/049) is explicitly a non-goal. Source of truth is in-repo (`config.py`, `docker-compose.yml`, #001–#012 decisions) → Confidence High. `.env.example` pulled into scope beyond the roadmap line (README + OVERVIEW) because it is the concrete onboarding artifact and is actively wrong. |
| 2026-07-05 | Initial cloud-fork roadmap: LLM → Claude, STT → OpenAI Whisper, TTS → Cartesia. Features #001–#015 derived from the hable-ya on-device → cloud-API migration analysis. |
| 2026-07-05 | Spec 001-cloud-llm-native-tools drafted (bundles #001–#006: Claude Sonnet 4.6 via Pipecat Anthropic service, native `log_turn` tool-calling with `tool_choice: auto`, handler rework, prompt/tool-def move, config swap, warmup replacement); #001–#006 → in-progress. Corrects #002: `tool_choice` forcing suppresses the spoken reply, so emission is `auto` + prompt, not forced. Assumes a prerequisite #000 port of the hable-ya runtime into this repo. |
| 2026-07-05 | #000: hable-ya runtime ported into this repo as the baseline (git-tracked files at HEAD; hable-ya's ROADMAP and historical spec dirs excluded). |
| 2026-07-05 | Spec 012-eval-rebaseline implemented; #012 → implemented. New `eval/claude_agent.py::call_claude` (native `log_turn` tool-calling); `run_eval.py` + `run_agent_eval.py` re-pointed at the Anthropic SDK (`--model` default `claude-sonnet-4-6`, `tool_mode="native"`); openai/llama.cpp/`enable_thinking`/`gemma-4-e4b` removed; outputs record model id not `base_url`. Judges/learner/scoring/fixtures/comparators untouched. Live smoke passed against the real API (Claude emits a native `log_turn`); 258 pytest passing; ruff + mypy clean. Gaps: no fixture JSONs are committed (batch-generated; `run_eval` fixture path dormant); judge/learner model bump deferred (cache-key invalidation). |
| 2026-07-05 | Spec 012-eval-rebaseline drafted on branch `spec-eval-rebaseline-012`. Exploration confirmed the on-device coupling is confined to the *model-under-test* drivers (`eval/run_eval.py`, `eval/agent/run_agent_eval.py`: `openai.AsyncOpenAI` → llama.cpp `gemma-4-e4b` + `enable_thinking`); the Opus judges, synthetic learner, `eval/scoring/*`, fixtures, and comparators are already Claude/agnostic and carry over. Spec re-points the two drivers at the Anthropic SDK with native `log_turn` tool-calling (`tool_mode="native"`, default model `claude-sonnet-4-6`), adds a shared `LOG_TURN_ANTHROPIC_TOOL`, and reframes `--minimal-prompt` as the cloud baseline-vs-full-prompt ablation. Judge/learner model bump deferred. Confidence Medium (no offline coverage of the under-test path → new offline adapter test + live smoke). #012 → in-progress. |
| 2026-07-05 | Spec 010-ondevice-cleanup implemented; #010 + #011 → implemented. Promoted `anthropic` to core (fixed the runtime-imports-but-only-in-extras bug — proven by `import api.main` on a no-extras `uv sync`); dropped `faster-whisper`/`piper-tts`/`jupyterlab` + the `finetune` extra; deleted `finetune/` (importers redirected to `hable_ya`), `download_model.py`, `benchmark_*`, `replay_placement.py`, the notebook, and cleaned mypy/ruff config. Gates surfaced two undeclared transitive deps (`jsonschema` → declared as dev dep; spaCy `es_core_news_sm` → re-downloaded manual step). 254 pytest passing; ruff + mypy clean. Eval `run_agent_eval.py` still references llama.cpp/`gemma-4-e4b` — #012's to re-baseline. |
| 2026-07-05 | Spec 010-ondevice-cleanup drafted on branch `spec-ondevice-cleanup-010-011` (bundles #010 + #011): drop `faster-whisper`/`piper-tts`/`jupyterlab` + the `finetune` extra from `pyproject`, promote `anthropic` to core (fixes a real bug — the runtime imports it but it was only in extras); delete the `finetune/` package (redirect its 3 `render_system_prompt` importers to `hable_ya.pipeline.prompts.render`, its real home), `download_model.py`, `benchmark_*`, `replay_placement.py`, and the fine-tune notebook; clean the matching mypy/ruff config. Coupling fully grep-mapped; Confidence High. #010 + #011 → in-progress. |
| 2026-07-05 | Spec 009-gpu-free-deploy implemented; #009 → implemented. Removed the llama.cpp GPU compose service + `app` nvidia reservation + `depends_on:llama`; dropped `bootstrap_cuda()` from `api/main.py` and deleted `hable_ya/cuda_bootstrap.py`; `config.py` now `load_dotenv()`s at import (`override=False`). Runtime is CPU-only end to end. Matched spec exactly (no divergences); closed the `.env`-loading gap from specs 001/007. `docker compose config` parses; 254 pytest passing; ruff + mypy clean. Dockerfile already CPU-slim (no change); dep pruning stays #010. |
| 2026-07-05 | Spec 009-gpu-free-deploy drafted on branch `spec-gpu-free-deploy-009` (#009 + the `.env`-loading gap from specs 001/007): delete the llama.cpp GPU compose service + `nvidia` reservations, remove `cuda_bootstrap.py` + its `api/main.py` call, and load `.env` via `python-dotenv` (`load_dotenv()` in `config.py`, `override=False`). Dockerfile already CPU-slim (no change). `cuda_bootstrap.py` removal pulled forward from #011. Confidence High (deletion + one dotenv line); #009 → in-progress. |
| 2026-07-05 | Spec 007-stt-tts-cloud implemented; #007 + #008 → implemented. `load_services` now builds `OpenAISTTService` (`gpt-4o-transcribe`) + `CartesiaTTSService` (`sonic-3`); config swapped to OpenAI/Cartesia keys + voice; `cartesia` dep added. Model path is CPU-only (unblocks #009). Live smoke round-tripped Cartesia→OpenAI verbatim; 248 pytest passing; ruff + mypy clean. Gaps surfaced (decision record): the app never loads `.env` (`Settings` reads OS env only — affects the anthropic key too; add `env_file`); `OpenAISTTService.run_stt` needs WAV + a set sample rate for standalone use. GPU/cuda_bootstrap removal → #009; dep removal + promotion → #010. |
| 2026-07-05 | Spec 007-stt-tts-cloud drafted on branch `spec-stt-tts-cloud-007-008` (bundles #007 + #008: OpenAI `gpt-4o-transcribe` STT + Cartesia `sonic-3` TTS replacing faster-whisper + Piper in `load_services`; new `openai_api_key`/`cartesia_api_key`/voice config; adds the `cartesia` dep; live smoke test). Same leaf-service swap pattern as spec 001 → Confidence High; only unknowns are the Cartesia Spanish `voice_id` + keys. GPU/cuda_bootstrap removal + dep cleanup deferred to #009/#010; #007 + #008 → in-progress. |
| 2026-07-05 | Spec 001-cloud-llm-native-tools implemented; #001–#006 → implemented. Divergences (see decision record): #003 landed as a `register_function` handler + a counting-only `LogTurnEmissionObserver` (old `HableYaToolHandler` removed), not a frame-parsing processor; #002 uses Anthropic's default `auto` (pipecat 0.0.108 doesn't forward `tool_choice` anyway); #005 disables thinking but `effort` isn't exposed by `AnthropicLLMService` (deferred to #013). A live two-turn spike (`scripts/spike_anthropic_tools.py`) confirmed text+tool_use co-emission, `run_llm=False` answering the tool call, and no next-turn 400. 241 pytest passing; ruff + mypy clean. Follow-ups surfaced: promote `anthropic` to core deps (#010); Step 8 live-session emission-rate/latency measurement is human-run. |
