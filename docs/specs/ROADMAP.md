# Roadmap

Cloud-API fork of [`hable-ya`](https://github.com/adrianmoses/hable-ya). Same
product — a voice-first Spanish acquisition agent that is simultaneously
conversational partner, pedagogical assessor, and adaptive engine — but the
three on-device models are replaced with managed APIs:

| Role | hable-ya (on-device) | habla (cloud) |
|---|---|---|
| LLM | fine-tuned Gemma 4 E4B via llama.cpp | **Claude** (Pipecat `AnthropicLLMService`) |
| STT | faster-whisper (CUDA) | **OpenAI Whisper API** |
| TTS | Piper | **Cartesia** |

Unchanged from hable-ya: Silero VAD + SmartTurn v3 (small local CPU/ONNX
models, kept in-process), the Pipecat pipeline shape, the Postgres + Apache AGE
learner model, and the learner-profile / theming / leveling logic. The
migration is scoped to the model boundary and its fallout.

| Field | Value |
|---|---|
| status | planned |
| created | 2026-07-05 |

## Features

| ID | Feature | Status | Spec |
|---|---|---|---|
| 001 | Claude LLM (`claude-sonnet-4-6`) via Pipecat `AnthropicLLMService`, replacing the llama.cpp `OpenAILLMService` (drop `base_url`/dummy `api_key`) | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 002 | Register `HABLE_YA_TOOLS` with the LLM context and emit `log_turn` via native structured tool-calling with `tool_choice: auto` (forcing suppresses the spoken reply — see spec Key Decision 3) | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 003 | Rework the tool handler to consume native function-call frames instead of buffering `LLMTextFrame`s and regex-parsing `log_turn(...)` / `[TOOL_CALL: log_turn]{...}` (reuse the existing `api_tool_calls` path in `parse_tool_calls`) | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 004 | System prompt: move the `log_turn` emission instruction into the tool definition; remove the inline plain-text-emission contract the fine-tune baked in | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 005 | Config: Anthropic model id + `ANTHROPIC_API_KEY`; remove `llama_cpp_url`, `llm_model_name`, and the Gemma `chat_template_kwargs.enable_thinking=false` hack (disable thinking / low effort for voice latency) | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 006 | Replace the llama.cpp warmup ping-loop with a lightweight managed-API health check (or drop it) | implemented | [001-cloud-llm-native-tools](001-cloud-llm-native-tools/spec.md) ([decision](001-cloud-llm-native-tools/decision.md)) |
| 007 | STT → OpenAI transcription API (`gpt-4o-transcribe`, replacing faster-whisper CUDA `medium`); Spanish language config | implemented | [007-stt-tts-cloud](007-stt-tts-cloud/spec.md) ([decision](007-stt-tts-cloud/decision.md)) |
| 008 | TTS → Cartesia (`sonic-3`, replacing Piper `es_ES-davefx-medium`); owner-supplied Spanish voice | implemented | [007-stt-tts-cloud](007-stt-tts-cloud/spec.md) ([decision](007-stt-tts-cloud/decision.md)) |
| 009 | Deployment: delete the llama.cpp GPU compose service and drop all `nvidia` GPU reservations — app container becomes CPU-only (Postgres + AGE `db` service unchanged); also removes `cuda_bootstrap.py` and loads `.env` via python-dotenv | implemented | [009-gpu-free-deploy](009-gpu-free-deploy/spec.md) ([decision](009-gpu-free-deploy/decision.md)) |
| 010 | Dependency cleanup: drop `faster-whisper`, `piper-tts`, the `finetune` extra (`torch`, `unsloth`, `transformers`), and `jupyterlab`; promote `anthropic` to core (`openai`/`cartesia` already core) | implemented | [010-ondevice-cleanup](010-ondevice-cleanup/spec.md) ([decision](010-ondevice-cleanup/decision.md)) |
| 011 | Remove on-device-only tooling: `finetune/` package, `download_model.py`, `benchmark_*` scripts, `replay_placement.py`, the fine-tune notebook (`cuda_bootstrap.py` removed in #009) | implemented | [010-ondevice-cleanup](010-ondevice-cleanup/spec.md) ([decision](010-ondevice-cleanup/decision.md)) |
| 012 | Re-baseline the `eval/` harness: validate that Claude + prompt reproduces recast + `log_turn` fidelity, replacing the fine-tuned-vs-untuned-Gemma comparator (keep the Opus recast/session judges) | implemented | [012-eval-rebaseline](012-eval-rebaseline/spec.md) ([decision](012-eval-rebaseline/decision.md)) |
| 013 | Cloud round-trip latency re-benchmark: measure network TTFT vs local, re-tune `smart_turn_stop_secs` / VAD `stop_secs` for the added hop | implemented | [013-cloud-latency](013-cloud-latency/spec.md) ([decision](013-cloud-latency/decision.md)) |
| 014 | API resilience & cost: rate-limit handling, retry/backoff (tenacity already present), and per-turn token-cost observability | planned | — |
| 015 | Product/docs update: on-device → cloud posture — learner utterances now leave the device (privacy), README, OVERVIEW non-goals | implemented | [015-cloud-docs](015-cloud-docs/spec.md) ([decision](015-cloud-docs/decision.md)) |
| 016 | Session auth & single-session enforcement: gate the public `/ws/session` WS endpoint (token/API-key), enforce the single-tenant one-active-session assumption the shared-LLM `register_function` handler already depends on (concurrency cap / reject second connection), complete startup fail-fast for OpenAI/Cartesia keys + `CARTESIA_VOICE_ID` (today only Anthropic is verified; the others crash on first turn), and make `/health` reflect live cloud-API reachability (today it re-checks only the DB and returns `200 ok` while sessions are broken). Removes the open cost-DoS on the metered paid APIs — the one true public-exposure blocker. | implemented | [016-session-auth](016-session-auth/spec.md) ([decision](016-session-auth/decision.md)) |
| 018 | Web frontend auth + deploy: the `web/` Vite SPA is a dev-only artifact predating the cloud deploy — nothing builds/serves it in prod (Caddy only reverse-proxies the API), and its WS client opens `/ws/session` with no token so #016's fail-closed auth rejects it (`1008`). Wire the session token into the client (subprotocol/`?token=`, no server change), serve the built SPA behind the existing prod Caddy, and refresh the stale (llama.cpp/Gemma) `web/README.md`. No new UI surface. | implemented | [018-web-frontend-deploy](018-web-frontend-deploy/spec.md) ([decision](018-web-frontend-deploy/decision.md)) |
| 017 | Deployment hardening: non-root multi-stage Dockerfile (`uv sync --no-dev`, install `es_core_news_sm` so vocab tracking doesn't silently degrade, add a `HEALTHCHECK` wired to `/health`); a prod compose overlay (restart policy, resource limits, app healthcheck, db not host-exposed, reverse-proxy + TLS for `wss://`); replace the hardcoded `hable_ya:hable_ya` DB creds / empty-string secret defaults with injected secrets + config-layer validation; a GHCR image build-push CD job (CI is test-only today); a `pgdata` backup/restore path (none exists) and a redeploy/migration runbook (migrations currently run coupled to app boot; `scripts/init_db.py` exists as a standalone step but is unwired). | implemented | [017-deploy-hardening](017-deploy-hardening/spec.md) ([decision](017-deploy-hardening/decision.md)) |
| 019 | Learner-progress read API: the learner model is fully *written* (per-turn `log_turn` → Postgres relational tables + AGE graph; placement/leveling band changes at session end) but nothing outside the dev-gated `/dev/learner` + `/dev/observations` endpoints can *read* it. Add authed production HTTP endpoints exposing the profile snapshot (band, sessions completed, error/vocab aggregates), session history, band history, and recent themes — the data bridge every stubbed frontend surface in #020 needs. | planned | — |
| 020 | Web frontend: wire the dead surfaces to live data. Home's nav links `Progreso`/`Historial`/`Ajustes`, the profile avatar, six recent-topic cards, and "ver todo →" are inert (`Home.tsx:112-134,511,527-560` — no `href`/`onClick`, or a "próximamente" toast, while styled `cursor: pointer`); the stats tiles (racha / nivel / última sesión) and Session's `NIVEL A2` badge are static placeholders. Build the progress/history/settings screens on #019 (these are the #046-deferred surfaces #018 explicitly kept out of scope); interim quick fix: stop dead links from looking clickable. | planned | — |
| 021 | Learner identity: the UI greets a hardcoded "Ana" (`Home.tsx:176`, placeholder pending the learner model) and the backend has no user concept — a schema-enforced singleton learner (`learner_profile CHECK (id = 1)`), ephemeral `uuid4` session ids, and a single shared session token. Minimum: owner-configurable learner name/profile surfaced in the UI (stays single-tenant, consistent with OVERVIEW's "not multi-tenant" non-goal). Explicitly decide whether multi-user accounts remain a non-goal — supporting them needs a schema migration (drop the `id=1` CHECK), per-user auth, and per-learner session routing. | planned | — |
| 022 | Knowledge-graph read path (or honest downgrade): the AGE graph is upserted on every turn but write-only — no runtime code ever queries it; all live adaptivity (prompt profile, theming, leveling) is driven by the relational aggregates (`learner_profile`, `turns`, `error_counts`, `vocabulary_items`). Either wire graph reads into adaptation (e.g. error-pattern neighborhoods informing theme/prompt selection) or re-document the graph as an inspection-only artifact; also fix README's broken `turns` query (`created_at` → `timestamp`; no `l1_reliance_score` column exists). | planned | — |

## Status Values

- `planned` — not yet started
- `in-progress` — spec written, implementation underway
- `implemented` — decision record complete
- `deprecated` — removed from product

## Revision History

| Date | Change |
|---|---|
| 2026-07-12 | Post-deploy product audit added #019–#022 (all planned). Trigger: three user-visible gaps on the deployed app — dead frontend links, a default learner name, and no visible session logging / skill adaptation. Findings: the backend learner loop is **already wired and load-bearing** (every native `log_turn` tool call runs a per-turn Postgres transaction — `turns` row, `error_counts`, spaCy-lemmatized `vocabulary_items`, AGE graph upserts — with `runtime_turns.jsonl` as a redundant dev sink; the system prompt is DB-adaptive once a placement `band_history` row exists, and `end_session` runs real placement/leveling band writes), but it is **entirely invisible**: no production read API exists (only dev-gated `/dev/learner` + `/dev/observations`), so the SPA ships only the two #046 screens (Home, Session) with every other surface a static stub — `Progreso`/`Historial`/`Ajustes`/avatar/topic-cards/"ver todo" dead (`Home.tsx`), stats tiles and the `NIVEL A2` badge hardcoded, learner name hardcoded "Ana". Identity is a schema-enforced singleton (`learner_profile CHECK (id = 1)`, no user concept — consistent with the single-tenant non-goal but unconfigurable). One claim/reality gap: the AGE knowledge graph is written every turn but never read — adaptivity is relational-only (plus a broken README `turns` SQL example). Split into #019 (read API), #020 (frontend surfaces), #021 (learner identity), #022 (graph read path or downgrade). Suggested order: #019 → #020; #021 and #022 independent; the perceived "no adaptation" should mostly dissolve once #019/#020 surface the existing loop. |
| 2026-07-10 | Spec 018-web-frontend-deploy implemented; #018 → implemented. Shipped on branch `spec-web-frontend-deploy-018` (recommended option for every Open Question, no server-side change): token pasted once → `sessionStorage` and carried on the `Sec-WebSocket-Protocol` subprotocol (`client.ts`/`token.ts`; Home paste field gates the CTA; `Session.tsx` clears a `1008`-rejected token so Home re-prompts); `handle`-block `Caddyfile` (WS/health reverse-proxied, static SPA with `try_files` fallback — mutually exclusive so the file_server can't shadow the upgrade); `web/Dockerfile` (Node → `caddy:2`, `dist/` at `/srv`) published by `release.yml` as `<repo>-web` + a `ci.yml` web build gate; `web/README.md` rewritten for the cloud stack. **Two implementation discoveries:** (1) the SPA build was already broken on a fresh checkout — `Home.tsx` imported `web/src/lib/health.ts` but the repo `.gitignore`'s bare `lib/` rule (Python template) silently ignored `web/src/lib/`, so the file was never committed; fixed by anchoring the rule to `/lib/` and restoring the `useHealth` hook (CI had no web job to catch it — now it does); (2) a rejected token needed a client recovery path, not specified by #016/#018. Verified locally: `npm run build` green, prod `compose config` + `caddy validate` pass, web image builds with the SPA at `/srv`, and an HTTP routing run confirmed `/` + deep-links serve the SPA while `/health` + `/ws/session` route to the proxy (502 against a dead upstream = not static-shadowed); `git grep` clean of token literals under `web/`. Deferred to a human on a deployed host (Medium-confidence live spikes needing a public domain + real keys): the served-page authenticated `wss://` turn through Caddy and the same over real TLS. See [decision](018-web-frontend-deploy/decision.md). |
| 2026-07-10 | Spec 018-web-frontend-deploy implementation underway on branch `spec-web-frontend-deploy-018`; #018 → in-progress. Open Questions resolved with the operator's recommended options: **OQ1** = Option B (paste-once token → `sessionStorage`, never bundled) transported via the **`Sec-WebSocket-Protocol` subprotocol** (kept out of URLs/access-logs; `token_urlsafe` chars are valid subprotocol tokens, so no `?token=` fallback needed); **OQ2** = Option A (Caddy `file_server` serves the built `dist/`, `/ws/session*` + `/health` still `reverse_proxy`'d — via mutually-exclusive `handle` blocks so the static handler can't shadow the WS upgrade); **OQ3** = a custom Caddy+SPA image (`web/Dockerfile`, Node build → `caddy:2` with `dist/` at `/srv`) built + pushed by `release.yml` as `<repo>-web`, so the operator pulls a prebuilt image rather than hand-building on the host. Landed: restored the missing `web/src/lib/health.ts` `useHealth` hook (`Home.tsx` imported it but it was never committed here, breaking the build — a build-gate prerequisite); `web/src/lib/token.ts` + a paste-once token field on `Home.tsx` (CTA gated on token presence); `client.ts` attaches the token as the sole WS subprotocol; `Session.tsx` passes it and clears the stale token on a `1008` reject; `handle`-block `Caddyfile`; `web/Dockerfile` + `.dockerignore`; `docker-compose.prod.yml` `caddy` service → `ghcr.io/adrianmoses/habla-web:latest`; `release.yml` `web-image` job (scoped gha cache) + `ci.yml` `web` typecheck/build gate; `web/README.md` rewritten for the cloud stack. **No** server-side change (`_extract_token` already accepts the subprotocol). Deferred live spikes (need a public domain + real keys): served-page authenticated `wss://` turn through Caddy, and `GET /` SPA + `/ws/session` upgrade coexistence (no handler shadowing). Decision record to follow via `ss-decision`. |
| 2026-07-10 | Spec 018-web-frontend-deploy drafted on branch `spec-web-frontend-deploy-018`; #018 → planned. Surfaced during the first Hetzner deploy: the repo's `web/` Vite+React SPA (from the ported spec #046) is unusable on a deployed host — (1) nothing builds or serves it (Dockerfile/compose/Caddy/CI never reference `web/`; prod Caddy only reverse-proxies the API's `/ws/session`+`/health`, no `StaticFiles` mount), (2) its WS client opens a bare `new WebSocket('/ws/session')` with no token, so #016's fail-closed auth rejects every connection with `1008`, and (3) `web/README.md` still documents the deleted on-device stack (llama.cpp/Gemma/spec-021). #017 had explicitly deferred frontend deploy as a Non-Goal. Spec scopes the two hard blockers + doc refresh only (no new UI surface — the #046-deferred screens stay deferred): attach the session token to the handshake (recommend `Sec-WebSocket-Protocol` subprotocol over `?token=` to avoid logging it; no server change — `_extract_token` already accepts both), and serve the built `dist/` behind the existing prod Caddy (`file_server` + SPA `try_files`, `reverse_proxy` retained for the WS+health paths). Confidence Medium: mechanics standard but two open questions gate implementation — the token-exposure model (bundle-baked `VITE_SESSION_TOKEN` vs. a paste-once `sessionStorage` field; recommend the latter for a public URL) and serve-location (Caddy `file_server` vs. FastAPI `StaticFiles`; recommend Caddy to keep the API image lean). Depends on #016 (token contract) + #017 (Caddy edge); orthogonal to #014. |
| 2026-07-08 | Spec 017-deploy-hardening implemented; #017 → implemented. Shipped all six workstreams on `spec-deploy-hardening-017` (7 commits: spec + WS1–WS6). WS1 multi-stage non-root Dockerfile (`uv sync --no-dev --no-install-project`, `es_core_news_sm` baked in, uid 10001, `libstdc++6`, curl-free urllib `HEALTHCHECK`); WS2 `docker-compose.prod.yml` overlay (Compose `!reset`/`!override` to strip host ports, GHCR image, limits, Caddy auto-ACME `wss://`) + base `./data`→named `appdata` volume, base stays TLS-free; WS3 `require_secure_db_credentials` lifespan fail-fast on empty/placeholder DB password (dev opt-out `allow_default_db_credentials`, base true/prod false) + 6 tests; WS4 `release.yml` GHCR CD separate from test-only `ci.yml`; WS5 AGE-safe `pg_dump`/`pg_restore` scripts; WS6 `docs/RUNBOOK.md`. Building surfaced two design misses the plan didn't catch: `eval/` is a RUNTIME dep of `hable_ya` (must ship in the image) and pipecat downloads NLTK `punkt_tab` at first import (pre-baked at build). The spec's central Medium-confidence risk resolved decisively — a plain `pg_dump -Fc` round-trips the Apache AGE graph cleanly (extension + `ag_graph`/`ag_label` via `pg_extension_config_dump` + label tables + vertex rows all survived dump→fresh-DB→restore), so no volume-snapshot fallback needed. 295 pytest passing (52 db tests skip without db), +6 new, ruff+mypy clean; Docker build + non-root import + both compose configs + backup/restore all verified locally. Deferred live spikes (need real keys / a public domain): healthy-boot end-to-end, `wss://`-through-Caddy turn, GHCR push. Spec gap → follow-up candidate: move the shared `eval.fixtures.schema`/`eval.scoring.recast` symbols into `hable_ya` so the runtime image needn't ship `eval/`. |
| 2026-07-07 | Spec 017-deploy-hardening drafted on branch `spec-deploy-hardening-017`; #017 → in-progress. Closes the deployment-readiness pair (#016 was the security blocker; #017 is the operational hardening that assumes it). Six independent workstreams: (1) multi-stage non-root Dockerfile (`uv sync --no-dev`, bake `es_core_news_sm` so vocab tracking stops silently degrading to `[]` at `vocabulary.py:44`, `HEALTHCHECK` reusing #016's `/health` semantics unchanged); (2) `docker-compose.prod.yml` overlay layered over the dev-friendly base — restart policy, resource limits, app healthcheck, `db` host-port publish removed (internal-only), reverse-proxy + TLS terminating `wss://`; (3) inject the hardcoded `hable_ya:hable_ya` DB creds + extend #016's lifespan fail-fast to reject empty/placeholder creds outside dev (kept out of the `Settings` validator so keyless CI `Settings()` still builds); (4) a GHCR build-push CD job separate from test-only `ci.yml`; (5) `pg_dump`/`pg_restore` backup/restore scripts that round-trip the AGE graph; (6) a deploy/migration runbook (boot-coupled `upgrade_to_head()` today vs standalone `scripts/init_db.py`). Confidence Medium — mechanics are standard but each is a cross-cutting infra integration point needing live validation (non-root writability of `runtime_turns.jsonl`/spaCy cache, AGE-graph backup round-trip, real `wss://` handshake, placeholder-rejection without breaking CI). Resolved by owner: **Caddy + automatic ACME TLS**, terminated in the **prod overlay only** (local `docker compose up` on the base file stays plain `ws://localhost:8000` — no proxy/cert/domain, dev loop untouched); **boot-coupled migrations kept** (`upgrade_to_head()` at boot; `init_db.py` documented as the manual alt, not wired). Remaining questions also resolved: secret transport = **env injection** baseline (compose `secrets:` optional overlay, documented not default); image tagging = `sha-<short>` + `latest` on `main`, `v<version>` on git tags. Spec **approved** — ready to implement (workstream 1: the Dockerfile). Explicitly NOT #014 (no retry/backoff/cost-metering; `/health` stays reactive — the pipeline-internal `ErrorFrame` blind spot remains #014's). Scope: single-host compose only (no k8s/GitOps/managed-secrets); CD publishes the image, operator deploys per runbook. |
| 2026-07-07 | Spec 016-session-auth implemented; #016 → implemented. Shipped the four-layer design: per-session services (`build_session_services`) so the shared-LLM `register_function` clobber can't corrupt across connections (concurrency now safe → cap is policy, not a correctness lock); preemptive newest-wins cap with the lock held only for the pointer swap + identity-guarded clear (a stale session can never block a new client); `idle_timeout_secs` reaping (was `None`); and a fail-closed shared-secret auth gate (`secrets.compare_digest`, subprotocol/query token, never logged) checked before `accept()`. Plus lifespan fail-fast on empty OpenAI/Cartesia/`CARTESIA_VOICE_ID` (in the lifespan, NOT a `Settings` validator — CI builds `Settings()` keyless) and a reactive `degraded` `/health` signal. 341 pytest passing (52 previously-skipped DB tests ran with the db up), +13 new auth/enforcement unit tests, ruff+mypy clean. Live spike passed: token-gated session drove a full STT→Claude→Cartesia turn returning spoken Spanish (`out.wav` ~197 KB). Additive divergences (helpers extracted for testability; `make_test_wav.py` + a trailing-silence endpointing fix to `voice_client.py`/`make_test_wav.py` needed to run the spike; token docs; incidental `.env.example` 4.0→3.0 fix). Not live-exercised: two-client preemption + killed-client reap (covered by offline helper tests). Gap → #014: reactive `/health` can't see pipeline-internal `ErrorFrame` provider failures. |
| 2026-07-06 | Spec 016-session-auth drafted on branch `spec-session-auth-016`; #016 → in-progress. Design centers on removing the *need* for a session lock rather than building one: Layer 1 gives each session its own `log_turn` handler (per-session LLM service — cheap, no model load) so the shared-`register_function` clobber (`session.py:80`) can't corrupt across connections, demoting the single-session cap from a correctness lock to a cost policy; Layer 2 enforces the cap by **preemption** (newest connection evicts the incumbent) under a lock held only for the pointer-swap, never the session lifetime — so a stale/half-open session can never block a new client and there's no death-timeout to tune; Layer 3 sets `runner.py`'s `idle_timeout_secs` (currently `None`) to reap abandoned sessions proactively; Layer 4 adds a shared-secret auth gate checked pre-`accept()`. Plus startup fail-fast on empty OpenAI/Cartesia keys + `CARTESIA_VOICE_ID` (today only Anthropic is warmed) and a reactive `degraded` signal in `/health`. Confidence Medium — auth/fail-fast/idle-timeout are low-risk; the eviction + per-session-teardown path needs a live spike. Open questions: fail-closed-vs-open on unset token (proposed: fail-closed + dev opt-out), reactive-vs-active health probing (proposed: reactive), per-session isolation scope for STT/TTS (proposed: all three), token transport (subprotocol or query param). Overlaps #014 on cost visibility — #016 does the spend-bounding cap; #014 owns real token metering. |
| 2026-07-06 | Deployment-readiness assessment (post-#013, now CPU-only) added #016 + #017. Findings: DB bootstrap is genuinely production-grade — a fresh `docker compose up` auto-runs Alembic migrations, creates the AGE extension + graph, seeds nodes, warms the LLM, and serves with no manual steps; `.env` secret handling, DB/Anthropic startup fail-fast, and dev-endpoint gating are all solid. The blocker: `/ws/session` is public and unauthenticated on `0.0.0.0:8000` with no rate-limit / concurrency cap / cost metering → open cost-DoS on the paid APIs, compounded by the shared-LLM `register_function` clobber corrupting turn logging under concurrent connections (the single-active-session assumption is load-bearing but unenforced). Plus partial fail-fast (only Anthropic checked at boot), a `/health` that re-checks only the DB, and no container/compose/CD/backup hardening (root user, dev deps in image, no restart/limits/TLS, no image publish, no pgdata backup). Split into #016 (auth + single-session enforcement + fail-fast + honest health — the blocker) and #017 (image/compose/CD/backup hardening). #014's cost/retry scope partly overlaps #016's cost-metering; to be reconciled when #014/#016 are specced. Recommend #016 first. |
| 2026-07-06 | Spec 013-cloud-latency implemented; #013 → implemented. Built `scripts/benchmark_latency.py` (per-stage cloud TTFB harness), `PerStageLatencyObserver` (live per-stage `latency_debug` split from Pipecat `MetricsFrame`s, wired via a new `build_observers()` helper), `hable_ya/runtime/latency.py` (pure p50/p95 stats), + offline tests (274 passing). Live run (20 iters, `013-cloud-latency/latency.json`): STT 711/1229, LLM TTFT 1179/1581, TTS 161/204 ms (p50/p95); summed network floor ≈2.05s p50 / 3.0s p95. Key finding: that floor (LLM TTFT + STT dominated) already exceeds the p50≤1.5s / p95≤2.5s target on the network legs alone, so endpointing tuning cannot hit it — re-tuned only to not add to the floor: `smart_turn_stop_secs` 4.0→3.0 (trim on-device carry-over ceiling; only bites on uncertain/trailing learner turns), `vad_stop_secs` kept 0.5 (evidence-backed no-op). Divergences: observer wired via extracted `build_observers()` (testable); TTS TTFB measured via a capture pipeline, not direct `run_tts()` (streaming Cartesia pushes audio downstream, doesn't return it). Spec gap → follow-up candidate: sub-target latency needs faster STT/LLM or streaming-partial STT (a #013 Non-Goal). Outstanding human check: live `voice_client.py`+`latency_debug` session to confirm 3.0s doesn't cut off learner pauses. |
| 2026-07-06 | Spec 013-cloud-latency drafted on branch `spec-cloud-latency-013`; #013 → in-progress. Exploration confirmed the turn-taking defaults (`smart_turn_stop_secs=4.0`, `vad_stop_secs=0.5` in `config.py`) are on-device carry-overs never re-tuned for the cloud hop, and that instrumentation is partial: `latency_debug` → `UserBotLatencyObserver` logs only end-to-end ms, while `PipelineParams(enable_metrics=True)` already emits per-service `MetricsFrame`/`TTFBMetricsData` that goes uncaptured. Spec adds three deliverables — a `scripts/benchmark_latency.py` harness (direct-`Services`, per-stage STT/LLM-TTFT/TTS-TTFB p50/p95, replacing the deleted on-device `benchmark_latency.py`), a per-stage `latency_debug` breakdown in `runner.py` sourced from `MetricsFrame`, and an evidence-gated re-tune of the two `stop_secs` defaults — plus fixing a stale `voice_client.py` llama.cpp docstring. Confidence Medium: mechanics are High but the tuning decision depends on live-measured p50/p95 and an owner-confirmed latency budget (Q1), and the "vs local" comparison is a documented historical reference, not a reproducible A/B (Q3). |
| 2026-07-06 | Spec 015-cloud-docs implemented; #015 → implemented. Docs + `.env.example` only (zero code — `git status` shows only those files + the spec dir). `.env.example` rewritten to mirror `config.py` (dropped `LLAMA_CPP_URL`/`WHISPER_*`/`PIPER_*`/gemma/`HF_TOKEN`; added the three provider keys + `CARTESIA_VOICE_ID`); README retitled `habla`/cloud with a working `docker compose up` run path (Download/Serve-model sections removed, History note added); OVERVIEW/ARCHITECTURE posture, dependency, data-flow, and constraint sections corrected, the "Not a cloud-hosted service" non-goal inverted, and a **privacy** statement added (audio→OpenAI, transcript→Anthropic, reply→Cartesia leave the device). Two extra migration-caused dangling refs fixed in passing (OVERVIEW's `finetune/format.py` DPO citation; ARCHITECTURE's eval-run llama.cpp leg → Anthropic SDK). Scoped to cloud posture only; a full re-baseline of the `inferred` design docs (stubbed-runtime language predating specs 029/049) is flagged as a follow-up. Verified: grep gate clean, `.env`↔`config.py` diff exact, `docker compose config` parses; CI-scoped ruff + mypy clean, 258 pytest passing. Closes the #001–#015 migration series. |
| 2026-07-06 | Spec 015-cloud-docs drafted on branch `spec-cloud-docs-015`; #015 → in-progress. Docs-only slice closing the migration series: all consumer-facing docs still describe the deleted on-device system (README's `download_model.py` + `docker compose up llama` + GPU/`HF_TOKEN` setup; `.env.example`'s `LLAMA_CPP_URL`/`WHISPER_*`/`PIPER_*`/gemma vars that `config.py` no longer reads; OVERVIEW's "Not a cloud-hosted service" non-goal; ARCHITECTURE's llama.cpp CUDA deps). Spec rewrites README + `.env.example` + the posture/deps sections of OVERVIEW/ARCHITECTURE to the cloud stack, and records the new **privacy** posture (learner audio→OpenAI, transcript→Anthropic, reply→Cartesia now leave the device). Scoped to the cloud posture only — a full re-baseline of the `status: inferred` design docs (stubbed-runtime language predating specs 029/049) is explicitly a non-goal. Source of truth is in-repo (`config.py`, `docker-compose.yml`, #001–#012 decisions) → Confidence High. `.env.example` pulled into scope beyond the roadmap line (README + OVERVIEW) because it is the concrete onboarding artifact and is actively wrong. |
| 2026-07-05 | Initial cloud-fork roadmap: LLM → Claude, STT → OpenAI Whisper, TTS → Cartesia. Features #001–#015 derived from the hable-ya on-device → cloud-API migration analysis. |
| 2026-07-05 | Spec 001-cloud-llm-native-tools drafted (bundles #001–#006: Claude Sonnet 4.6 via Pipecat Anthropic service, native `log_turn` tool-calling with `tool_choice: auto`, handler rework, prompt/tool-def move, config swap, warmup replacement); #001–#006 → in-progress. Corrects #002: `tool_choice` forcing suppresses the spoken reply, so emission is `auto` + prompt, not forced. Assumes a prerequisite #000 port of the hable-ya runtime into this repo. |
| 2026-07-05 | #000: hable-ya runtime ported into this repo as the baseline (git-tracked files at HEAD; hable-ya's ROADMAP and historical spec dirs excluded). |
| 2026-07-05 | Spec 012-eval-rebaseline implemented; #012 → implemented. New `eval/claude_agent.py::call_claude` (native `log_turn` tool-calling); `run_eval.py` + `run_agent_eval.py` re-pointed at the Anthropic SDK (`--model` default `claude-sonnet-4-6`, `tool_mode="native"`); openai/llama.cpp/`enable_thinking`/`gemma-4-e4b` removed; outputs record model id not `base_url`. Judges/learner/scoring/fixtures/comparators untouched. Live smoke passed against the real API (Claude emits a native `log_turn`); 258 pytest passing; ruff + mypy clean. Gaps: no fixture JSONs are committed (batch-generated; `run_eval` fixture path dormant); judge/learner model bump deferred (cache-key invalidation). |
| 2026-07-05 | Spec 012-eval-rebaseline drafted on branch `spec-eval-rebaseline-012`. Exploration confirmed the on-device coupling is confined to the *model-under-test* drivers (`eval/run_eval.py`, `eval/agent/run_agent_eval.py`: `openai.AsyncOpenAI` → llama.cpp `gemma-4-e4b` + `enable_thinking`); the Opus judges, synthetic learner, `eval/scoring/*`, fixtures, and comparators are already Claude/agnostic and carry over. Spec re-points the two drivers at the Anthropic SDK with native `log_turn` tool-calling (`tool_mode="native"`, default model `claude-sonnet-4-6`), adds a shared `LOG_TURN_ANTHROPIC_TOOL`, and reframes `--minimal-prompt` as the cloud baseline-vs-full-prompt ablation. Judge/learner model bump deferred. Confidence Medium (no offline coverage of the under-test path → new offline adapter test + live smoke). #012 → in-progress. |
| 2026-07-05 | Spec 010-ondevice-cleanup implemented; #010 + #011 → implemented. Promoted `anthropic` to core (fixed the runtime-imports-but-only-in-extras bug — proven by `import api.main` on a no-extras `uv sync`); dropped `faster-whisper`/`piper-tts`/`jupyterlab` + the `finetune` extra; deleted `finetune/` (importers redirected to `hable_ya`), `download_model.py`, `benchmark_*`, `replay_placement.py`, the notebook, and cleaned mypy/ruff config. Gates surfaced two undeclared transitive deps (`jsonschema` → declared as dev dep; spaCy `es_core_news_sm` → re-downloaded manual step). 254 pytest passing; ruff + mypy clean. Eval `run_agent_eval.py` still references llama.cpp/`gemma-4-e4b` — #012's to re-baseline. |
| 2026-07-05 | Spec 010-ondevice-cleanup drafted on branch `spec-ondevice-cleanup-010-011` (bundles #010 + #011): drop `faster-whisper`/`piper-tts`/`jupyterlab` + the `finetune` extra from `pyproject`, promote `anthropic` to core (fixes a real bug — the runtime imports it but it was only in extras); delete the `finetune/` package (redirect its 3 `render_system_prompt` importers to `hable_ya.pipeline.prompts.render`, its real home), `download_model.py`, `benchmark_*`, `replay_placement.py`, and the fine-tune notebook; clean the matching mypy/ruff config. Coupling fully grep-mapped; Confidence High. #010 + #011 → in-progress. |
| 2026-07-05 | Spec 009-gpu-free-deploy implemented; #009 → implemented. Removed the llama.cpp GPU compose service + `app` nvidia reservation + `depends_on:llama`; dropped `bootstrap_cuda()` from `api/main.py` and deleted `hable_ya/cuda_bootstrap.py`; `config.py` now `load_dotenv()`s at import (`override=False`). Runtime is CPU-only end to end. Matched spec exactly (no divergences); closed the `.env`-loading gap from specs 001/007. `docker compose config` parses; 254 pytest passing; ruff + mypy clean. Dockerfile already CPU-slim (no change); dep pruning stays #010. |
| 2026-07-05 | Spec 009-gpu-free-deploy drafted on branch `spec-gpu-free-deploy-009` (#009 + the `.env`-loading gap from specs 001/007): delete the llama.cpp GPU compose service + `nvidia` reservations, remove `cuda_bootstrap.py` + its `api/main.py` call, and load `.env` via `python-dotenv` (`load_dotenv()` in `config.py`, `override=False`). Dockerfile already CPU-slim (no change). `cuda_bootstrap.py` removal pulled forward from #011. Confidence High (deletion + one dotenv line); #009 → in-progress. |
| 2026-07-05 | Spec 007-stt-tts-cloud implemented; #007 + #008 → implemented. `load_services` now builds `OpenAISTTService` (`gpt-4o-transcribe`) + `CartesiaTTSService` (`sonic-3`); config swapped to OpenAI/Cartesia keys + voice; `cartesia` dep added. Model path is CPU-only (unblocks #009). Live smoke round-tripped Cartesia→OpenAI verbatim; 248 pytest passing; ruff + mypy clean. Gaps surfaced (decision record): the app never loads `.env` (`Settings` reads OS env only — affects the anthropic key too; add `env_file`); `OpenAISTTService.run_stt` needs WAV + a set sample rate for standalone use. GPU/cuda_bootstrap removal → #009; dep removal + promotion → #010. |
| 2026-07-05 | Spec 007-stt-tts-cloud drafted on branch `spec-stt-tts-cloud-007-008` (bundles #007 + #008: OpenAI `gpt-4o-transcribe` STT + Cartesia `sonic-3` TTS replacing faster-whisper + Piper in `load_services`; new `openai_api_key`/`cartesia_api_key`/voice config; adds the `cartesia` dep; live smoke test). Same leaf-service swap pattern as spec 001 → Confidence High; only unknowns are the Cartesia Spanish `voice_id` + keys. GPU/cuda_bootstrap removal + dep cleanup deferred to #009/#010; #007 + #008 → in-progress. |
| 2026-07-05 | Spec 001-cloud-llm-native-tools implemented; #001–#006 → implemented. Divergences (see decision record): #003 landed as a `register_function` handler + a counting-only `LogTurnEmissionObserver` (old `HableYaToolHandler` removed), not a frame-parsing processor; #002 uses Anthropic's default `auto` (pipecat 0.0.108 doesn't forward `tool_choice` anyway); #005 disables thinking but `effort` isn't exposed by `AnthropicLLMService` (deferred to #013). A live two-turn spike (`scripts/spike_anthropic_tools.py`) confirmed text+tool_use co-emission, `run_llm=False` answering the tool call, and no next-turn 400. 241 pytest passing; ruff + mypy clean. Follow-ups surfaced: promote `anthropic` to core deps (#010); Step 8 live-session emission-rate/latency measurement is human-run. |
